# MAP — Kingshot_Toolbox

> Cartographie du projet. **Point d'entrée avant toute tâche** : lis ce fichier, puis n'ouvre que les fichiers concernés.

---

## 1. Vue d'ensemble

- **Live** : https://kingshottoolbox.com/
- **Repo** : `Aistra91-KS/Kingshot_Toolbox` (anciennement `Hub-Kingshot` : `/hub-kingshot/` → `/Kingshot_Toolbox/` sur GitHub Pages, puis la **racine** de `kingshottoolbox.com`). Le site ne dépend plus de son chemin de déploiement — cf. la règle `<base href>` relatif au §9.
- **Type** : site statique hébergé sur **GitHub Pages** (pas de backend, pas de build).
- **Stack** : HTML / CSS / JS **vanilla** (aucun framework, aucun bundler) + **GitHub Actions** (notif Discord).
- **Langues** : **FR / EN**, bascule à chaud (aucun rechargement).
- **Architecture** : Hub (catégories) → Outils. Site mono-jeu (Kingshot). Toute la navigation est pilotée par un manifeste unique `js/site-config.js`.
- **Persistance** : `localStorage` uniquement (clés centralisées dans `js/storage-keys.js`), export/import JSON via `js/backup.js`.

---

## 2. Arborescence

```
Kingshot_Toolbox/
├── index.html                    Hub (page d'accueil) : grille de cartes par catégorie — **écrite en dur** dans le HTML (cf. §9), `hub.js` la re-rend ensuite depuis `SITE`
├── research_calc.html            Outil Recherches (arbres Croissance/Éco/Combat)
├── truegold_calc.html            Outil TrueGold (planif. amélioration bâtiments)
├── waracademy.html               Outil Académie de Guerre (planif. recherches troupes TG)
├── beartrap_calc.html            Outil Piège à Ours (répartition des marches)
├── vikings.html                  Outil Vikings (répartition troupes/défense)
├── caserne.html                  Outil Caserne / Héros — source des héros
├── masters.html                  Outil Experts / Masters (beta) — affinités & skills
├── shop_calc.html                Sommaire des Boutiques (beta) — 3 familles : Événement / Permanentes / Coffres
├── pets.html                     Outil Familiers (promenade verticale : fiches pets le long d'un sentier)
├── about.html                    À propos : qui est derrière l'outil, données locales, mentions (page statique bilingue)
├── changelog.html                Nouveautés : historique des versions (rendu depuis data/changelog.json)
├── 404.html                      Page introuvable : autonome (styles inline, liens en `/` absolu — servie par Pages à n'importe quelle profondeur), `noindex`, jamais dans le sitemap
│
├── favicon.ico                   Icône multi-tailles (16/32/48) à la racine — convention attendue par les robots qui ne lisent pas le HTML ; jamais dans le sitemap
├── sitemap.xml                   Plan du site : 1 ligne `<url><loc>` par page publiée (68 URLs), **toutes sans `.html`** (cf. la règle des adresses au §9) — soumis à Google Search Console
├── _config.yml                   **Seul rôle : dire ce que GitHub Pages ne doit PAS publier** (`tools/`, `tests/`, les .md de travail). Sans lui, Pages recopie tout le dépôt à la racine du domaine — les gabarits et les partials se seraient servis en `text/html`, dupliquant le contenu des pages qu'on demande à Google de classer. Le site n'utilise rien d'autre de Jekyll : ni front matter, ni Liquid, ni gabarit
├── robots.txt                    Ouvre tout aux robots et déclare l'URL du sitemap (portée réelle : cf. §9)
├── llms.txt                      Sommaire du site pour les robots d'IA (format llmstxt.org) : résumé, puis les pages classées par section. Ni page ni sitemap — un fichier texte à tenir à jour quand une page est ajoutée ou retirée (la jumelle `/fr/` y est citée à côté de son anglaise)
├── google55512fe842dbeeaf.html   Jeton de vérification Google Search Console — ne pas supprimer, ne pas ajouter au sitemap
│
├── fr/                           **Pilote « une URL par langue »** (cf. §4) : pages servies en français en dur, hors du modèle EN + bascule JS du reste du site
│   └── shop/theater-shop.html    Jumelle française de `shop/theater-shop.html` — `<base href="../../">`, `hreflang` réciproque, rendu épinglé en FR
│
├── shop/                         Une page par boutique (structure calquée sur database/*, cf. §5)
│   ├── items.html                Valeur des objets : référentiel gemmes (ex-onglet « Data Item »)
│   ├── items-euro.html          Prix réel des objets : le relevé € / $ en lecture seule, le pack d'origine, et les 3 règles qui corrigent le relevé
│   ├── theater-shop.html         Événement · Magasin du Théâtre — **1re boutique à paliers** (même objet vendu 3 fois à 3 prix, cf. §6) et **1re page à jumelle française** (cf. §4)
│   ├── blizzard-shop.html        Événement · Magasin du Blizzard
│   ├── dragons-caravan.html      Événement · Caravane du Dragon
│   ├── adventure-stall.html      Événement · Stand d'Aventure
│   ├── polar-shop.html           Événement · Magasin Polaire
│   ├── summit-contest-champion.html   Événement · Ligue Suprême (Champion)
│   ├── summit-contest-ordinary.html   Événement · Ligue Suprême (Ordinaire)
│   ├── golden-ball-shop.html     Événement · Ballon d'or (terminé — page conservée en archive)
│   ├── arena.html                Permanente · Arène
│   ├── alliance-championship.html     Permanente · Championnat d'Alliance
│   ├── swordland.html            Permanente · Swordland
│   ├── kingdom-of-power.html     Permanente · Royaume du Pouvoir
│   ├── tidal-shop.html           Permanente · Magasin des Marées
│   ├── trial-shop.html           Permanente · Magasin du Défi
│   ├── gear-boost-chest.html     Coffre · Boost d'Équipement
│   └── war-chant-chest.html      Coffre · Chant de Guerre
│
├── css/
│   ├── style.css                 Feuille principale (thèmes, header, hub, contrôles, tables, boutons, responsive) + surcouches BDD (mobile : scroll contenu + 1ʳᵉ colonne figée ; `.hl-x` = « X » doré ; `.db-section` tables compactes + en-têtes num. à droite). NB : les styles de cartes objet `.shop-item-card`/`.sic-*`/`.shop-card-grid` (~40 lignes) ne sont plus référencés depuis le passage des boutiques au tableau
│   ├── db.css                    Styles partagés des pages Base de Données (extrait des <style> inline ; classes .db-index/.db-wide/.db-cards-sm + scoping .table-container vs .db-section — cf. §5) ; plus la barre de filtres `.db-toolbar`/`.db-search`/`.db-count` et les groupes `.wg-name`/`.wg-meta` de la page Recherches avancées
│   ├── shop.css                  Styles Boutiques (préfixe `.sx-`) : sommaire par famille, cartes+vignettes, badges de statut, en-tête de page boutique, podium, **tableau `.sx-table` + mode édition** ; plus `.sc-item-img` et `.shop-toolbar`, les raccourcis de bas de page `.sx-reflinks` vers les deux référentiels, la pastille de palier `.sx-tier`, le repli « Voir plus » du sommaire (`.sx-grid.is-clamped` + `.sx-more`, cf. §5), et la valorisation d'événement (préfixe `.sxe-`, cf. §5)
│   ├── waracademy.css            Styles spécifiques Académie de Guerre (préfixe .wa-)
│   └── pets.css                  Styles page Familiers (scène « sentier », décor CSS, DA nature distincte)
│
├── js/
│   ├── site-config.js            ★ Manifeste unique : identité (name/home), catégories, outils, registre d'icônes Lucide
│   ├── storage-keys.js           ★ Source unique des clés localStorage + safeParse()
│   ├── profiles.js               ★ Profils (comptes multiples) : registre kt_profiles + proxy transparent sur localStorage (données métier rangées par profil kt::<id>::<clé>) + migration + API window.Profiles. Chargé juste après storage-keys.js (avant tout script de page)
│   ├── lang.js                   ★ GlobalLang : get/set langue, applyI18n(dict), event 'langChanged'
│   ├── header.js                 ★ Header contextuel généré depuis SITE + thème + modales globales
│   ├── footer.js                 ★ Pied de page global (Discord / À propos / Nouveautés + version + mention fansite) — injecté sur toutes les pages sauf pets.html
│   ├── changelog.js              Rendu de la page Nouveautés (fetch data/changelog.json)
│   ├── help.js                   Module d'aide générique (bouton "?", modale, bandeau, tooltips) — la modale accepte des **chapitres** `sections[]` en plus de `steps` (cf. §9)
│   ├── modal-tabs.js             Onglets mobiles des modales Caserne/Experts (panneaux [data-mtab], < 820px)
│   ├── backup.js                 Sauvegarde globale (export/import .json par module)
│   ├── feedback.js               Formulaire de retour joueur — **chargé à la demande** par `header.js` au 1er clic, référencé dans AUCUNE page
│   ├── hub.js                    Rendu de la grille du hub depuis SITE
│   ├── research_script.js        Logique Recherches (fetch research_db.json)
│   ├── truegold_script.js        Logique TrueGold (fetch truegold_db.json + masters_db.json)
│   ├── wa_optimizer.js           Optimiseur Académie de Guerre (3 modes) — logique pure
│   ├── waracademy.js             UI Académie de Guerre : arbre CSS grid + connecteurs SVG (fetch truegold_war_db.json)
│   ├── beartrap.js               Logique Piège à Ours (fetch heroes_db.json + lit caserne via localStorage)
│   ├── vikings.js                Logique Vikings (réutilise formations Piège à Ours via localStorage)
│   ├── caserne.js                Logique Caserne (fetch heroes_db.json)
│   ├── masters.js                Logique Experts (fetch masters_db.json)
│   ├── shop-core.js              ★ Socle Boutiques : i18n partagé, chargement des 5 shopcalc_*.json **en `cache:'no-cache'`** (`scFetchData` — revalidation forcée, cf. §9), helpers, vignettes, compte à rebours, calcul des lignes
│   ├── shop_calc.js              Sommaire des boutiques : hydrate les cartes écrites en dur (vignette, statut, compteur)
│   ├── shop-page.js              Rendu d’une page boutique (window.SHOP_SLUG) : en-tête, podium, tableau (1 ligne/objet) + mode édition (crayon) ; pose le mode d’emploi, extensible par `window.spHelpExtras(cfg)` (cf. §9)
│   ├── shop-event.js             Valorisation d'un événement (section `#sp-event`) : grille des achats par jour, gains cumulés, % récupéré. Chargé UNIQUEMENT par les pages ayant un `data/events/<slug>.json` (cf. §5) — soit Stand d'Aventure, Caravane du Dragon et Magasin du Théâtre
│   ├── shop-theater.js          Magasin du Théâtre : prolonge la section événement par l'optimiseur d'amulettes (moteur du tirage + rendu), branché par `window.seExtras` (cf. §6) ; écrit aussi son chapitre du mode d'emploi via `window.spHelpExtras` (cf. §9)
│   ├── shop-items.js             Page « Valeur des objets » (référentiel gemmes éditable)
│   ├── shop-items-euro.js        Page « Prix réel des objets » (relevé € / $, LECTURE SEULE)
│   ├── db-masters.js             Rendu des pages BDD Experts (fetch masters_db.json ; window.MASTER_ID)
│   ├── db-pets.js                Rendu des pages BDD Familiers (fetch pets_db.json ; window.PET_ID)
│   └── pets.js                   Logique Familiers (promenade verticale scroll-jack ; fetch pets_db.json ; sélecteur de niveau → palier/skill/coûts ; i18n GlobalLang)
│
├── data/                         Données consommées par les calculateurs (JSON éditées à la main)
│   ├── research_db.json          720 lignes (arbres de recherche, coûts/temps par palier)
│   ├── truegold_db.json          Bâtiments TrueGold (rangeData, bldgMap, config, référence paliers)
│   ├── truegold_war_db.json      Académie de Guerre : {meta, scoring, trees} — généré depuis un CSV
│   ├── truegold_war_advanced_db.json  Académie de Guerre « Advanced » : 92 techs / 1010 niveaux (coûts par niveau, prérequis, palier TG)
│   ├── heroes_db.json            34 héros (génération, rareté, type, skills bilingues)
│   ├── beartrap_joiners_db.json  Tier-list joiners Piège à Ours : rang (S>A>B>C>D) par génération de serveur (IDs héros)
│   ├── masters_db.json           6 experts (paliers d'affinité, passif, skills)
│   ├── shopcalc_items.json       91 objets (valeur en gemmes) — référentiel
│   ├── shopcalc_classic.json     Boutiques classiques (contenu + coûts)
│   ├── shopcalc_events.json      Boutiques d'événement (avec endsAt)
│   ├── shopcalc_chests.json      Coffres personnalisés (composition)
│   ├── shopcalc_euro.json        Relevé € des packs payants (ADMIN, lecture seule) — 58 objets relevés + 6 calculés
│   ├── pets_db.json              14 familiers / 7 générations : skill (nom/desc + valeurs/palier) + coûts nourriture/avancement — bilingue {EN,FR}
│   ├── changelog.json            Historique des versions du site (rendu par changelog.html) — bilingue {EN,FR}
│   └── events/                   Contenu d'un ÉVÉNEMENT (missions, packs, paliers), 1 fichier par boutique concernée
│       ├── adventure-stall.json  Stand d'Aventure : 6 missions/jour, pack gratuit, piste gratuite, 7 paliers, 8 packs payants
│       ├── dragons-caravan.json Caravane du Dragon : 6 missions/jour, 5 missions saisonnières, prime gratuite, 6 packs — ni monnaie secondaire ni palier
│       ├── theater-shop.json   Magasin du Théâtre : 5 packs payants (1/jour, 6 jours) + 21 paliers d'exploration + `_meta.startsAt/endsAt` (fenêtre de jeu) + `missions[]` ; ni mission, ni pack gratuit, et aucune monnaie versée (cf. §6)
│       └── fantasy-theater.json Mécaniques du TIRAGE du Théâtre (coûts par étage, échelle de garantie, sauts, Fanstars, jetons par étage) — l'événement, pas la boutique
│
├── database/
│   └── buildings/                Base de données bâtiments (pages statiques, tables inline)
│       ├── index.html            Sommaire des bâtiments
│       ├── town-center.html      Table d'amélioration Centre-ville
│       ├── embassy.html          Ambassade
│       ├── command-center.html   Base de commandement
│       ├── war-academy.html      Académie de Guerre (bâtiment)
│       ├── infirmary.html        Infirmerie
│       ├── barracks.html         Quartiers
│       ├── stable.html           Écurie
│       └── range.html            Stand de Tir
│
│   └── waracademy/               Base de données recherches Académie de Guerre (pages statiques, table depuis JSON)
│       ├── index.html            Sommaire des 3 arbres + les recherches avancées
│       ├── infantry.html         Table recherches Infanterie
│       ├── archer.html           Table recherches Archers
│       ├── cavalry.html          Table recherches Cavalerie
│       └── advanced.html         Recherches avancées d'Or Véritable (92 techs, filtres + recherche)
│
│   └── masters/                  Base de données Experts (index + 1 page/expert, tables depuis JSON)
│       ├── index.html            Sommaire des 6 experts
│       └── <expert>.html         valora, pan, roman, cassia, guinevere, wilson (fetch masters_db.json)
│
│   └── pets/                     Base de données Familiers (index + 1 page/familier, tables depuis JSON)
│       ├── index.html            Sommaire des 14 familiers
│       └── <pet>.html            gray-wolf, lynx, bison, … (fetch pets_db.json)
│
├── img/                          Assets (WebP partout, sauf favicons logo/ en PNG, SVG icônes)
│   ├── logo/                     favicon.svg, favicon-32.png, favicon-96.png, apple-touch-icon.png, og-image.png (aperçu 1200×630 des partages de liens), logo-512.png (logo Organization du JSON-LD) (PNG conservés : fallback favicon + icône iOS non compatibles WebP) — déclarés en dur dans le `<head>` de chaque page, cf. §9
│   ├── buildings/                Vignettes bâtiments (.webp)
│   ├── WarAcademy/               Icônes recherches Académie de Guerre (.webp)
│   ├── heroes/                   Portraits héros (.webp)
│   ├── Master/ + MasterSkill/    Portraits experts + icônes skills experts (.webp)
│   │   └── Master/hd/            Portraits pleine page 600×800 pour `masters.html` uniquement (cf. §5)
│   ├── skills/ + widgetname/ + widgetskill/  Icônes skills héros & widgets (.webp)
│   ├── Item/                     Icônes objets boutique/ressources (.webp) — **nom de fichier = `name.EN` de l'objet** (`scImg()`), carré ~100–200 px, fond transparent. **17 objets n'ont pas encore d'icône** (skins, VIP, emote, mégaphone…) : la case reste vide, sans erreur
│   ├── shops/                    Vignettes de boutique `<slug>.webp`, format **16:9** (~640×360). Facultatif : sans fichier, la carte affiche la mosaïque des 4 meilleurs objets
│   ├── packs/                    Captures des packs payants `<id de pack>.webp` (34), affichées en aperçu au survol sur `shop/items-euro.html`. Redimensionnées à 560×760 max — elles ne servent qu'en bulle, la pleine résolution serait 25× plus lourde pour rien
│   └── pets/                     Familiers : portraits (.webp ×14) + sous-dossier skills/ (icônes compétence, .webp ×14)
│
├── tools/                        Outillage hors ligne, **jamais servi au visiteur** (cf. §11)
│   ├── build_pages.py            Générateur des 25 pages à gabarit — `--check` vérifie sans écrire
│   ├── templates/                Les deux gabarits : `shop.html`, `building.html`
│   ├── pages-shop.json           Ce qui change d'une page boutique à l'autre (17 entrées)
│   ├── pages-building.json       Idem pour les bâtiments (8 entrées) — **l'ordre du fichier est l'ordre de la nav**
│   └── partials/                 Le cas particulier d'UNE page : les 3 blocs du Théâtre, les 8 tableaux de niveaux
│
├── tests/                        Tests des moteurs de calcul — `node --test` à la racine, **zéro dépendance, zéro build** (cf. §10)
│   ├── harness.mjs               Charge les fichiers de `js/` tels quels dans un contexte `vm` (fetch sur disque, localStorage en mémoire, DOM inerte)
│   ├── shop-event.test.mjs       Valorisation d'événement : les contrôles chiffrés de §7, enfin rejouables
│   └── theater-draw.test.mjs     Moteur du tirage du Théâtre contre une simulation Monte-Carlo grainée
│
└── .github/
    ├── workflows/tests.yml             Workflow : lance `node --test` à chaque push et sur les PR
    ├── workflows/discord-announce.yml  Workflow : publie l'annonce (déclenché par le commit de announce.md)
    ├── scripts/announce.js             Script Node : lit announce.md → 2 embeds Discord (FR + EN)
    └── news/announce.md                Annonce en attente : en-tête `covers-until` + sections `## FR` / `## EN`
```

---

## 3. Pages

| Page | Rôle | JS liés | CSS | Données |
|---|---|---|---|---|
| `index.html` | Hub : cartes par catégorie | `hub.js` (+ socle) | `style.css` | — (lit `SITE`) |
| `research_calc.html` | Optimiseur de recherches | `research_script.js` | `style.css` | `research_db.json` |
| `truegold_calc.html` | Planif. bâtiments TrueGold | `truegold_script.js` | `style.css` | `truegold_db.json`, `masters_db.json` (affinité) |
| `waracademy.html` | Planif. recherches troupes TG | `wa_optimizer.js`, `waracademy.js` | `style.css`, `waracademy.css` | `truegold_war_db.json` |
| `beartrap_calc.html` | Répartition marches Piège à Ours | `beartrap.js` | `style.css` | `heroes_db.json`, `beartrap_joiners_db.json` (tier-list) + caserne (localStorage) |
| `vikings.html` | Répartition troupes Vikings | `vikings.js` | `style.css` | formations Piège à Ours (localStorage) |
| `caserne.html` | Gestion héros | `caserne.js`, `modal-tabs.js` | `style.css` | `heroes_db.json` |
| `masters.html` | Experts & affinités (beta) | `masters.js`, `modal-tabs.js` | `style.css` | `masters_db.json` |
| `shop_calc.html` | Sommaire des boutiques : 3 familles + accès au référentiel (beta) | `shop-core.js`, `shop_calc.js` | `style.css`, `db.css`, `shop.css` | `shopcalc_items/classic/events/chests/euro.json` (`scLoadAll()` les charge tous les 5) |
| `shop/<boutique>.html` | Une boutique : en-tête, podium, tableau (1 ligne/objet) + mode édition. **Deux lectures** au choix (pastilles 💵 $ / € — celle d'entrée — et 💎 Gemmes, cf. §6) | `shop-core.js`, `shop-page.js` (+ `shop-event.js` si la boutique a un fichier d'événement) | `style.css`, `db.css`, `shop.css` | idem + `shopcalc_euro.json` (via `window.SHOP_SLUG`) |
| `shop/theater-shop.html` | Le Magasin du Théâtre ajoute, **sous le détail des gains**, la section « Ce que valent tes amulettes » (`js/shop-theater.js`) : c'est la seule boutique dont la monnaie ne s'achète pas — packs et missions versent des Amulettes, un tirage les convertit en Jetons. Le module ne redit rien de la section événement (grille d'achat, missions, amulettes dépensées, paliers restent à `shop-event.js`) : il lit son calcul et n'ajoute que le tirage, d'où **trois saisies propres** — étage courant, compteur de garantie, barème de jetons. Sa barre d'outils reprend l'idiome `.sx-take` des steppers pour le groupe d'actions (même bordure, même hauteur — les boutons de `.sx-take` font 24 px de large, taillés pour un « − » : il faut les repasser en `width: auto`). Il **déplace** dans cette barre le champ « amulettes dépensées » de `shop-event.js` (déplacé, jamais recopié : le nœud garde son câblage) et le tient à jour tout seul via trois boutons « je viens de : échouer / monter / encaisser » | + `shop-theater.js` | + préfixe `.stx-` dans `shop.css` | + `events/fantasy-theater.json` |
| `fr/shop/theater-shop.html` | Jumelle **française** du Magasin du Théâtre : même rendu, mais HTML français en dur et langue épinglée par l'URL (pilote, cf. §4) | idem `shop/<boutique>.html` | idem | idem |
| `shop/items.html` | Valeur des objets : référentiel gemmes éditable. Renvoie en bas vers `items-euro.html` (`.sx-reflinks`, en dur) | `shop-core.js`, `shop-items.js` | `style.css`, `db.css`, `shop.css` | `shopcalc_items.json` |
| `shop/items-euro.html` | Prix réel des objets : prix unitaire en € ou $ + pack d'origine, dont le nom **ouvre l'image du pack en aperçu** (survol maintenu, focus clavier, ou clic/toucher qui l'épingle). **Lecture seule** (donnée admin), pastilles € / $, recherche par objet **ou par pack**. Ne liste que les objets qu'on sait chiffrer (**57 / 87** : 55 relevés dans un pack — dont 8 dont le prix est ensuite décidé par une règle —, + la Caisse Mythique **calculée**, + l'Accélérateur 8h que seul le **barème** chiffre) ; la note de bas de tableau annonce cette couverture, et un renvoi en bas de page mène à `items.html` (`.sx-reflinks`, en dur). Trois **pastilles** disent quand le prix ne sort pas du pack affiché — « Calculé », « Barème », « ×0,25 » (`.ie-tag`, cf. §9) —, et l'encadré sous le tableau (`#ie-derived`, `ieRenderRules()`) déroule les trois règles avec leur calcul chiffré | `shop-core.js`, `shop-items-euro.js` | `style.css`, `db.css`, `shop.css` | `shopcalc_items.json`, `shopcalc_euro.json` |
| `pets.html` | Familiers : promenade verticale (fiches pets) | `pets.js` + `header.js`, `lang.js`, `site-config.js` | `style.css`, `pets.css` (+ webfonts) | `pets_db.json` |
| `about.html` | À propos : présentation, données locales, mentions | inline + `header.js`, `footer.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | — (texte en dur, `data-en`/`data-fr`) |
| `changelog.html` | Nouveautés : versions datées + étiquettes de type | `changelog.js` + `header.js`, `footer.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | `changelog.json` (fetch) |
| `database/buildings/*.html` | Tables d'amélioration bâtiments | inline + `header.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | données inline (HTML) |
| `database/waracademy/{infantry,archer,cavalry}.html` | Tables recherches Académie (3 arbres) | inline + `header.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | `truegold_war_db.json` (fetch) |
| `database/waracademy/advanced.html` | Recherches avancées : 92 techs / 1010 niveaux, puces de catégorie + recherche texte | inline + `header.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | `truegold_war_advanced_db.json` (fetch) |
| `database/masters/*.html` | Fiches Experts : affinité, passif, compétences | `db-masters.js` + `header.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | `masters_db.json` (fetch) |
| `database/pets/*.html` | Fiches Familiers : compétence/palier, avancements, nourriture | `db-pets.js` + `header.js`, `lang.js`, `site-config.js` | `style.css`, `db.css` | `pets_db.json` (fetch) |

**Socle chargé sur toutes les pages outils** (ordre) : `site-config.js` → `storage-keys.js` → `profiles.js` → `lang.js` → `help.js` → *(script de page)* → `header.js` → `backup.js` → **`footer.js` (toujours en dernier**, cf. §5 « Pied de page »).
Les pages `database/buildings/*` et `database/waracademy/*` n'incluent que `site-config.js` + `lang.js` + `profiles.js` + `header.js` (pas de help/backup ; `profiles.js` y sert uniquement l'UI de profils du header — pas de données métier à isoler) ; `database/masters/*` et `database/pets/*` ajoutent en plus leur script de rendu dédié (`db-masters.js` / `db-pets.js`), qui pose `window.MASTER_ID` / `window.PET_ID` et gère l'i18n de la page (dict + `data-en`/`data-fr`). **Les 36 pages `database/*` chargent `css/style.css` puis `css/db.css`** (feuille partagée extraite des anciens `<style>` inline). `pets.html` charge `site-config.js` + `storage-keys.js` + `lang.js` + `header.js` + `pets.js` + `backup.js` (sauvegarde des niveaux via `STORAGE_KEYS.pets`), sans `help.js`, plus `css/pets.css` et deux webfonts Google (Cormorant Garamond + Karla).

**Conventions d'affichage des fiches BDD (Experts/Familiers)** — pilotées par `db-masters.js` / `db-pets.js` :
- **Placeholder « X » doré** : le « X »/« X% » des descriptions (la valeur qui change au palier) est mis en valeur via `<span class="hl-x">` (helper `highlightX()`, X isolé seulement — « XP » n'est pas touché). Style dans `style.css`.
- **Effets à deux valeurs → deux colonnes** : quand un `effect` d'Expert vaut `(a;b)` (deux X dans la phrase, cf. §6), `db-masters.js` éclate la colonne « Effet » en **« Effet 1 » / « Effet 2 »** (helpers `effIsDual()` / `effParts()` / `effHead()` / `effCells()`), valeurs en `.num` (compactes). Un effet simple reste sur une colonne `.c-eff`.
- **Lisibilité tableaux** : tables au contenu (`.db-section table { width:auto }`) avec en-têtes numériques alignés à droite (`th.num`) pour coller les titres aux valeurs ; sur mobile (≤ 820px) chaque table défile dans sa boîte (`.tbl-scroll`), 1ʳᵉ colonne (palier) figée, page sans débordement (cf. §5).
---

## 4. Système bilingue (i18n)

Deux mécanismes, tous deux réagissant à l'event global **`langChanged`** émis par `GlobalLang.set()` :

1. **Dictionnaire + `data-i18n`** (pages outils) :
   - Chaque script de page déclare un objet `const i18n = { FR:{clé:…}, EN:{clé:…} }`.
   - Dans le HTML : `<span data-i18n="clé">…</span>` (et `data-i18n-placeholder="clé"` pour les inputs).
   - `GlobalLang.applyI18n(i18n[lang])` remplace `textContent` / `placeholder`.
2. **Attributs inline `data-en` / `data-fr`** (pages `database/buildings/*`) :
   - `<td data-en="Bread" data-fr="Pain">Pain</td>` — un petit script inline applique `data-<lang>` sur `[data-en][data-fr]`.

**Langue du HTML servi = anglais.** Le texte écrit en dur dans les fichiers `.html` (et `<html lang="en">`) est celui de la langue **par défaut**, `EN` — le français est appliqué par le JS au chargement. C'est ce qu'exige le référencement : Googlebot indexe le rendu, qui est anglais, et un HTML français sous un `<title>` anglais envoyait des signaux contradictoires. Conséquence à connaître : un visiteur ayant choisi `FR` voit brièvement l'anglais avant la bascule (~40 ms en conditions normales, jusqu'à ~3 s en 3G bridée) — c'est le même phénomène qu'avant, dans l'autre sens, quand le HTML était français et la majorité des visiteurs en anglais. **Toute nouvelle page s'écrit donc en anglais en dur**, avec les `data-i18n` / `data-fr` qui portent la traduction.

**Exception — pilote « une URL par langue » (`fr/shop/theater-shop.html`).** Le modèle ci-dessus sert UN html anglais que le JS traduit : Googlebot, qui n'a pas de préférence, n'indexe donc jamais que l'anglais, et le contenu français n'existe pour lui nulle part. La page du Magasin du Théâtre teste l'autre approche, celle que Google attend d'un site bilingue : **deux adresses, une par langue**, qui se citent par `hreflang` (`en`, `fr`, `x-default` → EN) et portent chacune son propre `canonical`. La jumelle française est du HTML français en dur, `<html lang="fr">`, `<base href="../../">`.

Le basculement se joue en **six lignes inline dans la page `/fr/`**, jamais dans `lang.js` (60 autres pages en dépendent) :
- `GlobalLang.get()` est réécrit pour renvoyer `FR` — header, footer, tableau et boutique s'affichent en français **quelle que soit la préférence enregistrée du visiteur** : ici c'est l'URL qui décide, pas la préférence.
- `GlobalLang.set('EN')` ne retraduit pas la page, il **emmène vers la jumelle anglaise**. C'est ce que doit faire le bouton de langue dès qu'une langue = une adresse ; sans cela le bouton semblerait cassé.
- la préférence globale n'est écrite (`FR`) que si le visiteur **n'en avait aucune** : arriver par la porte française règle le site en français, mais un choix déjà fait est respecté.

La page anglaise, elle, ne change pas de comportement : un visiteur réglé en `FR` la voit toujours traduite par le JS. **Portée : cette page seulement.** Ne pas généraliser sans mesurer — chaque page dupliquée est une page à maintenir en double.

**Langue** : stockée sous `hub_lang` (clé propre à `GlobalLang`, défaut `EN`). Sur desktop, le header expose un **bouton rond « globe + code »** (`.app-header-lang`, jumeau visuel du bouton thème) qui bascule FR⇄EN via `GlobalLang.set()` (`hdrInitLangToggle`/`hdrUpdateLangCode` dans `header.js`) ; sous 820px et en mode condensé il est masqué (langue reprise dans le drawer / le panneau profil). Les boutons `.lang-btn` du hub restent synchronisés via `GlobalLang.applyToButtons` (`applyToSelect` demeure un utilitaire générique, plus utilisé par le header).

**Où ajouter une clé** :
- Page outil → ajoute la clé dans les blocs `FR` **et** `EN` de l'objet `i18n` du script concerné, puis pose `data-i18n="clé"` dans le HTML.
- Page bâtiment → soit ajoute au `dict {FR,EN}` inline + `data-i18n`, soit utilise directement `data-en`/`data-fr` sur l'élément.
- Libellé de navigation (nom d'outil/catégorie) → dans `site-config.js` (`name: {EN, FR}`), jamais en dur.

---

## 5. Charte graphique (DA) — « Royal Gold / Black Edition »

Or éclatant sur noir profond, turquoise pour la validation, rubis pour l'alerte. Définie par variables CSS dans `css/style.css` (`:root` = sombre, `[data-theme="light"]` = clair).

### Variables — thème SOMBRE (`:root`)
| Variable | Valeur | Usage |
|---|---|---|
| `--bg-dark` | `#0a0a0a` | Fond principal |
| `--bg-panel` | `#161616` | Panels / cartes |
| `--control-bg` | `#1f1f1f` | Contrôles |
| `--input-bg` | `#252525` | Inputs |
| `--text-light` | `#f0e8d5` | Texte principal (ivoire) |
| `--text-muted` | `#8a8378` | Texte secondaire |
| `--accent` | `#f5b840` | **Or** (liseré, titres, focus) |
| `--accent-hover` | `#d49820` | Or foncé (hover) |
| `--border` | `#2a2a2a` | Bordures |
| `--success` | `#4ecdc4` | Turquoise (validation / CTA) |
| `--warning` | `#ff8c42` | Orange (alerte) |
| `--table-header` | `#1c1c1c` | Entêtes de table |
| `--table-row-alt` | `#131313` | Alternance de lignes |
| `--growth-color` | `#4ecdc4` | Arbre Croissance |
| `--eco-color` | `#f5b840` | Arbre Économie |
| `--battle-color` | `#e74c5c` | Arbre Combat |
| `--box-bg` / `--box-header` | `#1c1c1c` / `#252525` | Cartes de recherche |
| `--step-bg` / `--step-hover` | `#161616` / `#202020` | Étapes de recherche |
| `--shadow` | `rgba(0,0,0,.6)` | Ombre |
| `--header-height` / `--header-offset` | `60px` / `80px` | Header fixe + décalage body |

### Variables — thème CLAIR (`[data-theme="light"]`)
`--bg-dark #f5f7fb` · `--bg-panel #ffffff` · `--control-bg #f0f4fa` · `--input-bg #ffffff` · `--text-light #1a1a1a` · `--text-muted #6b7280` · `--accent #c89020` (or antique) · `--accent-hover #a07418` · `--border #d4dce8` · `--success #00897b` · `--warning #e65100` · `--table-header #eaf0f8` · `--table-row-alt #f5f8fc` · `--growth #00897b` · `--eco #c89020` · `--battle #c62828` · `--box-bg #ffffff` · `--box-header #eaf0f8` · `--step-bg #f5f8fc` · `--step-hover #e8eff7` · `--shadow rgba(0,0,0,.08)`.

### Police & rayons
- **Police** : `'Segoe UI', Tahoma, Geneva, Verdana, sans-serif` (système, aucune webfont).
- **Rayons** : pas de variable dédiée — valeurs par composant : cartes `12px`, panels/tables `8px`, contrôles `6px`, inputs `4px`, boutons pilule `20–25px`, ronds `50%`.
- **Thème** : attribut `data-theme` sur `<html>`, stocké sous **`hub_theme`** (défaut `dark`). Bascule via `toggleHeaderTheme()`.

### Convention cartes (signature visuelle)
`.hub-card` = **liseré doré** en haut (`border-top: 3px solid var(--accent)`) + **reflet doré au survol** : pseudo-élément `::before` (dégradé `rgba(245,184,64,.08)`) qui **balaie de gauche à droite** (`left: -100% → 100%`, `transition .6s`) + `translateY(-5px)`. `overflow:hidden` obligatoire sur la carte. Ne pas réinventer d'autre effet décoratif.

### Exception assumée — page Familiers (`pets.html`)
`pets.html` **s'écarte volontairement** de la DA Royal Gold : scène « sentier » en palette nature/jour (verts + terre, valeurs `oklch`), carte info blanche translucide, **webfonts** Cormorant Garamond + Karla. Pastilles de la sidebar colorées **par génération** (`--gen-1…7`), étoiles de palier (`--star`), sélecteur de niveau + pips de valeurs propres à cette page. Header reste en DA standard. Choix validé ; tout est isolé dans `css/pets.css` + `js/pets.js`.
**Mobile (< 880px)** : la promenade laisse place à une colonne centrée qui défile — image (`.pet-flow-img`) au-dessus de la carte, toutes deux dans `#petFlow` (`display:contents` sur desktop, donc sans effet hors mobile). Le décor reste fixe derrière ; `.animals` et la parallaxe sont hors circuit. Le sentier devient une frise de pastilles numérotées (`.pp-dot .pp-n`) ancrée en bas ; navigation par balayage **horizontal** (le vertical est rendu au défilement de la fiche).


**Header — pastille profil + adaptatif (Option B)** : la zone droite du header porte une pastille profil (avatar à initiale + anneau coloré par profil, réutilise le mécanisme `.hdr-dd`). Sur desktop, quand la rangée d'outils est **rognée** (mesure : `need = cat + hdr-tools.scrollWidth` vs `hdr-center.clientWidth`, avec **hystérésis** anti-clignotement, cf. `hdrEvaluateAdaptive`), le header passe en `.hdr-condensed` : langue + thème quittent la barre et se replient dans le panneau profil (`.pfp-chrome`), rendant la place aux outils. Sous 820px la nav (dont langue/thème) vit dans le drawer, qui reçoit en tête un bloc profil. Toute l'UI profil est dans `header.js` (`hdrBuildProfile`, `hdrOpenProfilesModal`, `hdrInitAdaptive`…).

**Pied de page global (`.site-footer`, `js/footer.js`)** : injecté en fin de `<body>` sur **toutes les pages sauf `pets.html`** (promenade plein écran au défilement piloté — le pied de page y serait hors d'atteinte ; garde-fou `body.pets-page` dans le module, en plus de l'absence de balise `<script>`). Contenu : trois destinations (Discord · À propos · Nouveautés), la version (`SITE.version`) et la mention « fansite non officiel ». Conventions et pièges :
- **Le lien Discord n'apparaît que si `SITE.discord` est renseigné** (chaîne vide = deux liens seulement). Même mécanisme sur `about.html`, dont le bloc « Une erreur, une idée ? » reste `hidden` tant que la clé est vide. Ouvrir le Discord = **remplir cette seule clé**.
- **`<script src="js/footer.js">` va en DERNIER** dans la page : `backup.js` enregistre son listener `DOMContentLoaded` avant nous, donc son bouton mobile (`.backup-fab`, qui redevient `static` sous 600px) reste au-dessus du pied de page. Le module détecte ce bouton et pose `sf-has-fab` (réserve de 270px à droite, le bouton étant `fixed` en `z-index:9000` sur desktop).
- **Bande collée, pas carte flottante** : le pied de page touche les bords bas/gauche/droite, en écho au header (liseré doré 1px + ombre inversée). Les `margin: 44px -20px -20px` annulent le `padding: 20px` du body (§2) et **`width: calc(100% + 40px)` les compense** — surtout pas `flex-basis` : sous 768px le body passe en colonne (§15) et `flex-basis` y régit la *hauteur*, la bande débordait alors de 20px à droite. Le hub a un padding bas de **40px** : `.hub-body .site-footer` corrige sa marge d'autant, sinon la bande flotte 20px trop haut. Le contenu (`.sf-inner`) est centré sur 1240px pour tomber sous celui de la page.
- **Piège du body en flex** : `body:not(.hub-body)` et `.hub-body` sont en `display:flex` (§4/§5) — un pied de page enfant du body s'y placerait **à côté** de la sidebar. D'où `body { flex-wrap: wrap }` + `flex: 0 0 auto` sur le pied de page (sa largeur > la ligne le force seul sur la sienne), et `min-width: 0` sur `body > .main-content, body > .db-page` (sans lui, un contenu large ferait basculer la colonne de contenu **sous** la sidebar).
- **`.page-row` — le pied de page passe sous la sidebar, pas derrière** : un élément `sticky` se confine à son **conteneur de bloc**. Tant que c'était le `<body>`, `.sidebar` descendait jusque *dans* le pied de page. `ftrWrapPageRow()` (js/footer.js) regroupe donc, **sur les seules pages à sidebar**, la sidebar et le contenu dans un `<div class="page-row">` : le pied de page vient après cette rangée, donc toujours **dessous**. Le body y passe en `display:block` (`body.has-page-row:not(.hub-body)`) et c'est `.page-row` qui porte le `display:flex; gap:20px` (colonne sous 768px). **Décision de Paul, ne pas la défaire** : la sidebar garde sa hauteur pleine — si le contenu principal est plus court qu'elle, la rangée conserve la hauteur de la sidebar et le vide à droite est assumé. Deux approches essayées puis rejetées : donner un `z-index` au pied de page (il passe devant la sidebar et **bloque le bouton « Sauvegarde Globale »**) et raccourcir dynamiquement la sidebar via une variable CSS (rétrécissement désagréable sur les pages courtes). Le pied de page reste **volontairement non positionné** — ne pas lui remettre de z-index.
- **Ce que `.page-row` ne doit pas avaler** : seuls les enfants du body **en flux** sont déplacés. Le filtre écarte les `<script>`, le pied de page lui-même et tout ce qui est en `position:fixed`/`absolute` — header, drawer mobile + son overlay, modales de page (`#hero-modal`, `#master-modal`, `.modal-overlay`), modales de sauvegarde et de profils, et le `.backup-fab` sur desktop (sous 600px il redevient `static`, donc il rejoint la rangée, ce qui le laisse au-dessus du pied de page : c'est voulu). Un nouvel élément de chrome ajouté au body doit donc être `fixed`, sinon il finira dans la rangée.
- **Contraste** : les étiquettes de type et les pastilles de version sont du gras en 11px. En thème **clair**, l'or (`#c89020`) n'y donne que 2,6:1 — les règles `[data-theme="light"] .cl-tag/.cl-ver/.cl-latest/.sf-version` assombrissent ces teintes (or `#8a6410`, turquoise `#00695c`, orange `#b23c00`, tous ≥ 4,9:1). La charte reste inchangée ailleurs. De même, `.sf-link` a pour fond `--bg-dark` et non `--control-bg` : le sous-titre en `--text-muted` n'y tenait que 4,4:1.

**Recherches — Sélection rapide (prérequis automatiques)** (`research_calc.html`) : groupe sidebar `#selection-options-group`, visible sur les seuls onglets d'arbres (comme les Options d'affichage). Mode opt-in (case `#auto-reqs`, persistée avec le reste de `researchInputs`) pensé pour la première mise en place : **cocher** une recherche coche toute sa chaîne de prérequis (`cascadeCheckReqs` — niveaux inférieurs du même nom + fermeture récursive des `reqs`, mêmes règles qu'`isAvailable`) ; **décocher** décoche symétriquement ce qui en dépend (`cascadeUncheckDeps`, par passes jusqu'à stabilité). Un `showAppToast` annonce le nombre de cases modifiées en plus de celle cliquée (silence si 0). Pire cas réel : 102 prérequis cochés en un clic (Shield Upgrade VI niv. 6), ~2 ms. La note sous la case (`.group-hint`, `style.css`) porte des teintes dédiées : `--text-muted` ne tient que 4,4:1 sur `--control-bg`, insuffisant en 12px.

**Palier serveur (TrueGold)** : sélecteur `#serverTier` (TG3 / TG5 / TG8 / TG10, **défaut TG8**), 1ᵉʳ paramètre du groupe *Configuration*. L'âge du serveur ouvre les paliers par crans : au palier N, le dernier niveau existant en jeu est `TGN-0` (`TGN-1` n'est pas encore sorti). Le plafond ne s'applique **qu'à l'optimiseur** — `niveauOuvert(label, palier)` écarte les améliorations candidates dans `executerPlan()` ; le tableau garde tous les niveaux sélectionnables et les totaux par ligne sont inchangés. Les bâtiments **déjà au-dessus** du palier sont gelés (ils comptent toujours comme prérequis) et listés dans un bandeau ⚠️ en tête du plan — bandeau affiché aussi quand aucun scénario n'est possible. Persisté avec le reste sous `STORAGE_KEYS.truegold`.

**Plan d'amélioration TrueGold (`.tg-plan`)** : le résultat de l'optimiseur est affiché en **séries chronologiques** — des niveaux consécutifs d'un même bâtiment, numérotées dans l'ordre réel d'exécution. Un bâtiment **réapparaît** autant de fois que l'escalier des prérequis l'impose (Centre-ville ⇄ Ambassade + bâtiments de troupes) ; ne jamais regrouper par bâtiment, la liste deviendrait inapplicable en jeu. Chaque série est un `<details>` (`.tg-serie`) : l'en-tête donne totaux TG/TTG, durée, points KVK, statut et la mention `🔓 débloque #n …` (calculée en croisant les prérequis TG de la série suivante) ; le corps déplié contient le tableau niveau par niveau (`.tg-steps`, largeur au contenu, colonnes num. à droite, ombres de bord CSS pour le scroll mobile). L'état déplié survit aux re-rendus via `TG_OPEN_SERIES` (clé `bâtiment|départ>arrivée`).

**« Appliquer les modifications » (TrueGold + Académie de Guerre)** : les deux plans se terminent par un CTA doré (`.plan-apply` / `.plan-apply-btn`, `css/style.css`, partagé par les deux pages) qui réécrit la page comme si le plan venait d'être réalisé en jeu — niveaux atteints, stocks réduits des quantités théoriques, puis recalcul immédiat de la suggestion suivante. TrueGold applique aussi le creuset (TG débité, TTG crédité du gain moyen, compteur `transfoUtilisees` incrémenté) ; l'Académie applique poussière et accélérateurs. L'action **écrase des saisies** : elle passe donc par `showAppConfirm` avec un récapitulatif chiffré avant/après (`.apply-diff`), puis un `showAppToast` de succès. **Décisions tranchées par Paul, ne pas les inverser** : (1) l'étape laissée « en construction » / « en cours » monte quand même de niveau — ses ressources sont déjà payées et le joueur finira le chantier ; (2) le creuset est appliqué malgré son gain aléatoire (1,45 TTG en moyenne), l'avertissement invitant à corriger le TTG à la main ; (3) **pas d'annulation** — la confirmation suffit. La source de vérité est le dernier plan calculé (`TG_LAST_PLAN`, remis à `null` en tête de `SUGGERER_KINGSHOT` ; `lastPlan` côté Académie) : bouton et plan affiché naissent du même calcul, ils ne peuvent pas se désynchroniser.

**Optimiseur TrueGold — mode KVK résolu exactement** (`resoudreKVKExact`, `truegold_script.js`) : les modes *Max bâtiments* et *Score cible* restent gloutons (un niveau à la fois, meilleur poids immédiat) ; le mode **KVK (max points)**, lui, explore **toutes** les combinaisons. Le glouton y était structurellement piégé : il ne peut pas « investir » dans un bâtiment sans intérêt propre mais qui en débloque un bien plus rentable (l'Écurie TG7 ouvre le Centre-ville TG7-1), et deux bâtiments jumeaux au poids identique (Stand de tir / Écurie, mêmes coûts et mêmes temps) étaient départagés par le seul **ordre des lignes du tableau** — le joueur pouvait donc gagner des points en *décochant* un bâtiment. Ce qui rend l'exhaustif possible :
- **l'état se résume au vecteur de niveaux** (+ le bâtiment en construction) : TG, TTG et accélérateurs restants s'en déduisent, donc deux ordres menant aux mêmes niveaux sont un seul état → mémoïsation (valeur empaquetée en un nombre : `points × 16 + premier coup`, le chemin est relu par `reconstruire()` — garder un tableau par état coûtait des dizaines de Mo quand l'exploration est abandonnée) ;
- **le nombre de transformations n'a plus de boucle 0..100** : pour un plan donné, `transfosNecessaires()` donne en O(1) le minimum de transformations qui le finance (les deux stocks étant monotones). Le bloc TRIM ne s'applique donc plus aux plans exacts ;
- **la règle des files borne la profondeur** : le plan s'arrête dès 2 constructions lancées sans accélérateurs.
Le score ne dépendant pas de l'ordre, `regrouper()` réordonne le plan pour qu'il se lise en quelques séries (cf. `.tg-plan`) et `rejouer()` valide le résultat — sinon on garde l'ordre brut. **Garde-fou** : au-delà de `KVK_MAX_ETATS` (60 000) on rend la main au glouton, qui rejoue alors ses 3 stratégies **plus une variante par bâtiment écarté** (l'astuce manuelle du joueur, automatisée). Mesures : médiane 16 états, p99 21 000, `runCalculator` ~25 ms au pire sur 250 scénarios, ~150 ms sur les cas extrêmes qui déclenchent le repli.

**Optimiseur Académie de Guerre — mode KVK** (`wa_optimizer.js`) : **reste glouton** (un niveau à la fois, pas de retour arrière) — contrairement à TrueGold, l'exhaustif est hors de portée : les 3 arbres dépassent **2 millions d'états** dès 3 000 poussière, là où TrueGold en compte une centaine. Le mode KVK garde donc le meilleur plan parmi plusieurs candidats (`KVK_ORDERS`), et le même piège qu'à TrueGold y était présent : on gagnait des points en **décochant un arbre**. Deux ajouts le corrigent :
- **Ordre `'chain'`** : un niveau verrouillé par un *prérequis* (pas par le palier d'Académie, qui ne s'achète pas) est valorisé comme un **paquet** — tous les niveaux manquants + lui-même — dont la densité (points/poussière) est portée par sa première marche jouable. Sans ça le glouton ne pouvait jamais investir dans une suite de niveaux médiocres qui ouvre une bien meilleure affaire : le **nœud de palier de troupe** vaut 2 765 pts/poussière (le meilleur de la base) mais se cache derrière ~8 600 poussière de niveaux à 1 631 (les pires). Face à trois arbres quasi symétriques, l'ancien glouton entamait les trois chaînes et n'en finissait aucune. La part déjà payée sortant du calcul, une chaîne entamée devient de plus en plus attractive → elle est terminée. **Deux garde-fous obligatoires** : ne s'engager que si le paquet tient dans le budget restant **de poussière ET d'accélérateurs**, et ne retenir comme première marche qu'un niveau *appartenant* au paquet (un prérequis déjà satisfait est enregistré à coût nul, son niveau suivant est hors paquet et donc non financé).
- **Relance sur chaque arbre seul** : le plan optimal **concentre** — à 15 000 poussière il met 13 540 dans un arbre et laisse les deux autres au minimum. Aucun glouton réparti ne propose ça ; le même glouton limité à un arbre, si. Coût borné : 4 ordres × (1 + nb d'arbres) plans.

**Rôle de chaque mode (Académie de Guerre) — tranché par Paul, ne pas « corriger » l'un en l'autre** : *Max recherches* vise le **nombre** de recherches (il touche donc naturellement plusieurs arbres, les niveaux bon marché étant partout) ; *KVK* vise **les points, point final** — si le meilleur plan ne touche qu'**un seul** des arbres cochés, c'est la bonne réponse, pas un bug (la chaîne du palier de troupe ne paie qu'une fois terminée). Cocher un arbre dit « tu peux t'en servir », jamais « tu dois ». *Score cible* vise le moindre coût pour atteindre un score.

Résultat : **+4 à +6,5 %** de points dans la plage concernée, à ≤ 0,4 % de l'optimum réel (sauf gros budgets, ~1,8 % restants) ; sur 600 scénarios aléatoires, 123 améliorés (+12 % en moyenne) et **aucune régression**. Coût : 2 à 16 ms dans Chromium, derrière le debounce de 160 ms de la page. Les modes *Classique* et *Score cible* ne passent pas par ce chemin et rendent une sortie **identique**. *(Note : l'en-tête du module affirmait que le temps ne contraignait qu'en mode Classique — faux depuis plusieurs versions, corrigé.)*

**Portraits d'Experts — deux jeux d'images selon la taille d'affichage** :
- `img/Master/<Nom>.webp` (96–124 px) : vignettes d'origine, utilisées par les pages `database/masters/*` qui les affichent en 84–92 px. Nettes à cette taille, et légères. **Ne pas les remplacer.**
- `img/Master/hd/<Nom>.webp` (600×800) : portraits pleine page pour `masters.html` seul, servis par le helper `masterPortrait()` de `js/masters.js`. Le cadre `.master-portrait` fait 320 px de haut (et `#modal-header-bg` 400×150) : les vignettes d'origine y subissaient un agrandissement ×2,6 à ×3,2, d'où la pixellisation — que des `filter: blur()` masquaient au prix d'une image floue (supprimés).
- **Ratio 3:4 imposé** : `.master-portrait` fait 240×320, donc une source 600×800 n'est pas recadrée par `background-size: cover`. Les captures de jeu n'ont pas ce ratio ; elles sont calées en *contain* puis les bandes manquantes sont comblées en moyennant les pixels du bord (le fond doré est un dégradé lisse, la jointure est invisible). Rogner quelques pixels de bordure sur la source est nécessaire : les captures portent des salissures collées au cadre qui ressortiraient à la jointure.

**Boutiques — une page par boutique (`shop/*.html`)** : structure calquée sur `database/*` mais **hors** de `database/` — ces pages portent de l'état utilisateur (monnaie, stock restant) et chargent donc le socle complet (`storage-keys` → `profiles` → `help` → `backup`), là où les pages BDD sont en lecture pure. Conventions :
- **`shop_calc.html` reste l'URL du sommaire** (déjà indexée, référencée par `site-config.js`) : elle a changé de rôle, pas d'adresse. Trois familles y sont listées — Événement / Permanentes / Coffres — plus **deux** accès aux référentiels : `shop/items.html` (gemmes, éditable) et `shop/items-euro.html` (prix réel, lecture seule). Les deux cartes sont écrites **en dur** dans `shop_calc.html`, comme le reste du maillage interne.
- **Les cartes du sommaire sont écrites en dur** (liens, noms, monnaies via `data-en`/`data-fr`), comme les sommaires `database/*/index.html` : le maillage interne doit exister sans exécuter le JS. `shop_calc.js` n'hydrate que ce qui dépend des données ou de l'heure (vignette, nombre d'objets, badge de statut) puis réordonne les événements.
- **Une page boutique** = `<base href>`, `title`/`meta description`/`h1`/intro **en dur** (jamais reconstruits par le JS, sinon le bouton d'aide accroché après le `h1` saute au changement de langue), plus `window.SHOP_SLUG` ; tout le reste est rendu par `shop-page.js` dans des conteneurs vides (`#sp-thumb`, `#sp-facts`, `#sp-actions`, `#sp-cart`, `#sp-podium`, `#sp-table`, `#sp-switch`). Le bouton d'aide est ancré sur `#sp-facts`. Les raccourcis vers les autres boutiques (`#sp-switch`, classe `db-switch footer`) sont **sous le tableau** : on choisit une autre boutique après avoir lu celle-ci, pas avant. Juste au-dessus, le bloc `.sx-reflinks` — **écrit en dur dans les 16 pages** (maillage sans JS, cf. §9) — mène aux deux référentiels de valeurs : 💎 « Valeur des objets » (gemmes) et € « Prix réel des objets ». C'est la réponse au joueur qui doute d'un chiffre du tableau, placée là où la question se pose. Les deux pastilles reprennent les couleurs des deux lectures de la page (turquoise / or) ; le filet horizontal du bas de page est porté par `.sx-reflinks`, la nav qui suit perd le sien pour qu'il n'y ait qu'un trait.
- **Titre d'une boutique d'événement** : `Kingshot <boutique> - Best Deals & Gem Value`. Le titre mène par le **bénéfice** — ce que la page permet de décider —, pas par la monnaie : « <Monnaie> Costs » décrivait ce qu'on *paie* et non ce que la page apporte (classement au ratio, podium, stock restant, panier), et dépensait un tiers des ~60 caractères que Google affiche pour un terme qu'un joueur ne connaît **qu'une fois déjà dans l'événement**. La monnaie reste indexée par la `meta description`, l'intro, la barre de faits et l'en-tête du tableau : la sortir du titre ne la fait pas disparaître. Le **nom de l'événement** entre dans le titre quand il diffère de celui de la boutique (`Blizzard Shop` → titre `Blizzard Brawl Shop`, l'intro et la description nommant l'événement dans les deux langues) : c'est la requête la plus cherchée. Le `h1` et le fil d'Ariane, eux, gardent le nom **in-game** de la boutique.
- **Le contenu d'une boutique est un tableau, une ligne par objet** (`table.sx-table`) — pas une grille de cartes : à surface égale il montre 3 à 4 fois plus d'objets, et les colonnes se comparent en diagonale. Les colonnes racontent trois blocs : *ce que ça coûte* (Qté, Coût) → *ce que ça vaut* (Valeur gemmes, Ratio) → *ce que tu peux en tirer* (Restant, Max fin, Obtenable, Coût obt., événements seulement, séparés par un filet `.sep`). Seul le podium des 3 meilleures affaires garde des visuels.
- **Mode édition (crayon)** : les boutiques d'événement restent ajustables — quantité, coût, stock restant, ajout et retrait d'objets, réinitialisation à la version du fichier. Ces champs n'apparaissent **que** dans le mode édition (`SP_EDIT`, volontairement non persisté : personne ne doit rester en édition sans le savoir), déclenché par le bouton `.sx-edit-toggle`. La colonne de suppression est `position:sticky; right:0` — une action qui sort du champ au scroll horizontal est une action perdue.
- **Panier / budget (boutiques d'événement)** : `si.take` = nombre de **lots** retenus (un objet se vend souvent par lot, d'où la colonne « Qté » qui reste la taille du lot). `scComputeRows()` calcule en **deux passes** — d'abord le coût de la sélection, ensuite seulement le `canTake` de chaque ligne, qui dépend du solde restant donc de toutes les autres lignes. Quatre tuiles en tête (`#sp-cart`) : monnaie, dépensé, **restant**, valeur obtenue. Panier et mode édition **s'excluent** : l'un consomme la boutique, l'autre la corrige. Pas de remplissage automatique — écarté par Paul : le joueur choisit lui-même.
- **Re-rendu après saisie** : `spAfterEdit()` diffère la reconstruction d'un tick (`setTimeout 0`), et `spSnapshotTable()`/`spRestoreTable()` remettent en place le **défilement du `.table-container` et le focus** (`[data-i]` + `[data-role]` sur les contrôles du panier) : sans eux, chaque clic sur le « + » d'une ligne du bas ramenait le tableau en haut et faisait perdre le bouton sous le curseur. Remplacer le tableau depuis le `change` d'un champ détruit ce champ pendant que le navigateur traite son `blur` — ce qui lève une DOMException. Le report coalesce aussi les appels rapprochés.
- **Largeur de page — piège** : `body:not(.hub-body)` est en `display:flex` (style.css §282, héritage des pages calculateur à sidebar). Un `.db-page` sans largeur explicite y devient un **flex item dimensionné par son contenu** : sa largeur ne suit plus l'écran, et `repeat(auto-fill, …)` — qui exige une largeur connue — retombe sur **une seule colonne**. Le symptôme est sournois : le nombre de colonnes dépendait des **polices du système** (3 sous Linux, 1 sous Windows, à code identique). `shop.css` corrige avec `body:not(.hub-body) .db-page { width:100%; min-width:0 }`. Les pages `database/*` ont le même défaut latent, masqué par leurs tableaux larges qui forcent la largeur.
- **Récapitulatif du panier (`.sx-cartbar`)** : barre `position:sticky; bottom:0` placée **hors du `<table>`**, après `.table-container`. Ne pas la remettre en `<tfoot>` : Chromium ne colle un `tfoot` sticky qu'une fois le défilement entamé, donc elle serait invisible à l'arrivée sur la page. Son contenu est **aligné à gauche** parce que `.backup-fab` (style.css) est en `position:fixed` à droite avec `z-index:9000` et passera toujours devant.
- **Largeur des tables** : `style.css` cale les tables BDD sur leur contenu (`.db-section table.db-table { width:auto }`) ; `shop.css` rétablit `width:100%` pour `.sx-table` et laisse la colonne du nom absorber le reste, sinon une boutique à 6 colonnes laisse la moitié de la page vide.
- **Événement terminé = page conservée**. Supprimer une URL indexée est le seul vrai dégât SEO possible ici, et les boutiques reviennent d'une saison à l'autre. La carte du sommaire passe en grisé (`.sx-card.is-ended`, vignette en `grayscale`) **et reste cliquable** ; la page affiche un bandeau d'archive. Ajouter une boutique = 1 entrée JSON + 1 page + 1 carte au sommaire + 1 ligne `sitemap.xml` + 1 lien dans `llms.txt`.
- **Vignettes** : `img/shops/<slug>.webp` si le fichier existe, sinon **mosaïque de secours** des 4 objets de plus forte valeur de la boutique (`scThumbHtml`) — l'image réelle recouvre la mosaïque, un `onerror` la retire si elle manque. Aucune image à produire pour que le sommaire soit présentable.
- **« Voir plus » par catégorie (sommaire)** : chaque `.sx-grid` s'ouvre sur **deux lignes** de vignettes, le reste derrière un bouton. Le plafond suit le nombre de colonnes d'`auto-fill`, sinon « deux lignes » coupe au milieu d'une ligne : **4** (1 ou 2 colonnes, < 884px) · **6** (3 colonnes, 884–1159px) · **8** (4 colonnes, ≥ 1160px). Ces trois seuils sont ceux de la grille elle-même (`minmax(260px,1fr)` + `gap:16px`), **relevés au navigateur** — les déplacer sans les remesurer redonne des lignes coupées. **Aucun JavaScript** : `<input type="checkbox" class="sx-more-cb" hidden>` **avant** la grille (le sélecteur `~` ne remonte pas en arrière), `<div class="sx-grid is-clamped">`, puis `<label class="sx-more" hidden>`. Le CSS fait tout : la découpe (`nth-child`), le dépliage (`.sx-more-cb:checked ~`), le libellé affiché et l'existence même du bouton (`:has()` : rien à révéler = pas de bouton). ⚠️ **Deux pièges, tous deux payés une fois** : (1) replier depuis le JS après le chargement faisait **sauter la page** — CLS mesuré à **0,68 sur mobile** — et un `shop_calc.js` en cache laissait des cartes masquées derrière un bouton inerte, que le `<noscript>` ne rattrapait pas (le JS est actif, juste périmé) ; le repli doit donc tenir **dans le HTML et le CSS**, en place dès le premier pixel et sans script à périmer. (2) `hidden` sur la case ET le libellé est le **garde-fou du CSS en cache** : seule la feuille qui sait replier le lève (dans le bloc `@supports selector(:has(*))`), donc un CSS périmé ou un navigateur sans `:has()` donne la grille entière, sans case ni bouton parasites. Les quatre combinaisons neuf/périmé sont testées. Découpe **au nombre de cartes**, jamais à la hauteur : une ligne s'aligne sur sa carte la plus haute. Pas de dégradé de fondu — la coupe tombe pile entre deux lignes.
- **Place réservée aux vignettes (`.sx-thumb-slot`)** : le slot porte le **même `aspect-ratio: 16/9` que `.sx-thumb`**. Sans lui c'est un span de hauteur nulle, et l'arrivée des données faisait grandir chaque carte d'un coup : **CLS de 0,389 sur desktop**, très au-dessus du 0,1 que Google juge bon. Avec, **0,068**. Toute vignette posée en JS a besoin de sa place réservée en CSS.
- **Compte à rebours** : `scTimeLeft()`/`scTimeLeftTxt()` (affichage en J+H, rafraîchi chaque minute sur `[data-ends-at]`) est **distinct** de `scResetsLeft()` (nombre de réinitialisations 00h UTC restantes, qui multiplie le stock des objets à reset quotidien). Ne jamais fusionner les deux : l'un est cosmétique, l'autre entre dans les calculs. **Le tick réécrit le cadre, pas seulement le texte** : sur `[data-ends-at]`, trois attributs facultatifs disent à `scStartCountdowns()` quoi corriger quand l'échéance tombe — `data-ends-full` (dire « Événement terminé » plutôt que « Terminé »), `data-ends-state` sur l'élément qui porte la classe `live`/`urgent`/`ended` (la `.sx-card` ancêtre reçoit `is-ended` avec lui), `data-ends-label` sur le libellé « Fin dans : » à masquer. Sans eux, une page laissée ouverte à minuit affichait « Fin dans : Terminé » en orange. **Une seule pastille dans `spRenderHero()`, jamais deux markups** : le compteur reste branché même terminé, sinon l'état « terminé » est inatteignable sans rechargement. Aucun attribut n'est requis — un `shop-core.js` en cache les ignore et le compteur se comporte comme avant (cf. règle du cache ci-dessous). **Le compteur ne suffit pas** : au basculement, `scStartCountdowns()` émet l'événement **`endsStateChanged`** (seulement au changement d'état, jamais chaque minute), auquel `spRenderAll` et `scRefreshIndex` sont branchés comme à `langChanged` — sinon le bandeau d'archive, la colonne « Obtenable », le compteur « n en cours », l'ordre des cartes et le verdict du Théâtre restent périmés derrière un badge à jour. Un **événement**, pas un appel : une page au JS en cache ne l'écoute pas, et rien ne peut appeler un nom qui n'existe pas encore.

**Valorisation d'un événement (`js/shop-event.js`, section `#sp-event`)** — répond à « ce que l'événement m'a coûté vs ce qu'il m'a rapporté », en % d'argent réel. Une boutique n'a cette section que si `data/events/<slug>.json` existe **et** que sa page charge `shop-event.js` (aujourd'hui : Stand d'Aventure seul). Conventions :
- **Module optionnel, jamais bloquant** : au chargement, le module `fetch` son fichier ; en 404 il s'arrête sans rien afficher. `shop-page.js` ne le connaît que par `window.ShopEvent` (fonction `spNotifyEvent()` appelée dans `spRenderAll()` / `spAfterEdit()`) : une page boutique sans le script se comporte exactement comme avant. Le garde-fou de `seAfter()` est obligatoire — `spRenderAll()` s'exécute **avant** que le module ait fini de charger.
- **Le % vit entièrement dans l'échelle € / $** : dépense et valeur sont dans la même devise, donc la règle « aucun taux gemme↔euro » (§9) est respectée. La valeur en gemmes est affichée à côté, jamais divisée par la dépense. Un joueur sans achat n'a **pas** de % (division par zéro) : la tuile affiche « F2P ».
- **Le panier de la boutique compte dans la valeur** : c'est lui qui donne une valeur aux pièces d'événement (elles n'en ont aucune en propre — le JSON ne leur donne ni `gemValue` ni prix). D'où la notification croisée ci-dessus : tout geste sur le panier recalcule la section.
- **Crans de couleur du %** (`seTier`, décidés par Paul) : < 100 rouge · 100–120 orange · 120–140 neutre · 140–180 vert clair · 180+ vert. Le libellé (« Mauvaise affaire » … « Excellent ») accompagne **toujours** la couleur : jamais d'information portée par la seule teinte. Teintes propres au bloc en `.sxe-t-*` — le rouge `#e5484d` du reste de la page ne tient que 4,2:1 sur fond sombre et l'orange `--warning` que 3,4:1 en thème clair, insuffisants pour la réserve en 11px.
- **Montants en argent réel : `seFmtMoney()`, 2 décimales fixes** — surtout pas `scFmtEur()`, dont les décimales adaptatives sont faites pour des valeurs *unitaires* d'objet : un pack à 119,99 € s'y affichait « 120 € » et un pack à 0,99 $ « 0,990 $ ». `scFmtEur()` reste en usage pour la valeur des lots dans le détail des gains, par cohérence avec le tableau de la boutique.
- **Détail des gains : colonne « Provenance »** — chaque apport est tracé à la source dans `seAdd()` (5ᵉ argument) et la cellule (`seSrcCell`) montre les **deux plus gros apports en pastilles** + un « +n » tiret é, le détail complet vivant dans l'info-bulle de la case : une ligne de tableau n'a pas à devenir un paragraphe. Le tableau s'ouvre sur une **ligne de total** (gemmes + € des seules lignes cochées) et chaque objet porte une **case à cocher** : décoché, il reste listé (grisé, valeurs barrées) mais sort du total ET de la valorisation — exclusions persistées par événement (`excluded` dans le plan, clé = itemId ou `x:<label EN>` pour un objet hors référentiel ; les monnaies n'ont pas de case, rien à retirer). Piège de largeur : `.sx-table` élargit sa **2ᵉ colonne** à 100 % (le nom, dans une boutique) — ici la 2ᵉ colonne est « Qté », d'où le correctif `.sxe-rewards` qui rend la largeur à la Provenance, sans quoi les pastilles s'empilent et triplent la hauteur des lignes.
- **Report du budget explicite** : le bouton « Utiliser comme budget » recopie les pièces prévues dans « Ma monnaie » du tableau. Jamais automatique — la saisie du joueur ne s'écrase pas en silence.
- **Placement : sous le tableau** (décision de Paul). On lit d'abord la boutique, on fait les comptes ensuite.
- **Point d'extension `window.seExtras(host, c)`** : à la fin de `seRender()`, si une page a posé cette fonction, elle est appelée avec le nœud de la section et le calcul complet — **avant** la remise du focus, pour que les contrôles ajoutés en profitent aussi. C'est par là que le Théâtre greffe son optimiseur sans toucher au reste (7 lignes ajoutées à `shop-event.js`). **`seCompute(upTo)`** accepte une borne optionnelle : ce que le plan a versé aux `upTo` PREMIERS jours de l'événement, packs des jours suivants exclus (omise, le comportement est celui d'avant — tout l'événement). Exposée par `ShopEvent.compute(upTo)`, elle évite à un greffon de recopier les règles de la boutique pour distinguer « ce que j'ai déjà » de « ce que le plan versera encore ». Un module greffé ne recalcule rien : il lit `c`, et se redessine **seul** depuis son dernier `c` quand ce sont SES saisies qui changent — repasser par `ShopEvent.refresh()` rejouerait toute la valorisation pour rien. Il lui revient en revanche de restaurer le focus et l'état de ses `<details>`, que `seRender()` ne connaît pas. **Déplacer un contrôle de la boutique dans son propre bloc** est permis et préférable à le recopier — le nœud conserve ses gestionnaires — mais il faut le **détacher avant** d'écraser son `innerHTML`, sinon le module détruit en se redessinant un contrôle qui ne lui appartient pas.
- **Ajouter un événement** = 1 fichier `data/events/<slug>.json` + `<div id="sp-event"></div>` sous le tableau + `<script src="js/shop-event.js">` après `shop-page.js`. Rien d'autre : le module lit la boutique pour la monnaie et les libellés. Deux événements sont branchés (Stand d'Aventure, Caravane du Dragon).

**Modales de détail (Caserne / Experts)** : tiroir latéral 400px sur desktop, plein écran + onglets sous 820px. Les sections de `#modal-body` portent `data-mtab="<clé>"` ; `js/modal-tabs.js` génère la barre `.mtabs` et n'affiche qu'un panneau à la fois (classe `.mtab-on`). Un panneau vide masque son onglet. Ajouter une section = poser `data-mtab` + déclarer la clé dans `PAGES` du module.


## 6. Données

Toutes les données sont des **JSON éditées à la main** dans `data/` (pas de script générateur ni de CSV commités). Les textes sont bilingues via objets `{EN, FR}`.

| Fichier | Format / schéma |
|---|---|
| `research_db.json` | **Liste** de 720 entrées : `{Tree, Name, Fr Name, Level, Time (d/h/m/s), coûts ressources…}` (18 champs). |
| `truegold_db.json` | **Objet** : `rangeDataTTG`, `bldgMap`, `defaultBuildings`, `dbDataRaw`, `levelsReference`, `buildingsConfig`. L'ordre de `defaultBuildings` est aussi l'**ordre d'affichage canonique** du tableau : `normalizeBuildingOrder()` (truegold_script.js) le réapplique après `loadData()`, sinon l'ordre resterait figé dans les sauvegardes des joueurs existants. |
| `truegold_war_db.json` | **Objet** `{meta, scoring, trees}`. `meta.warAcademyMaxLevel`; `scoring = {pointsPerDust:1000, pointsPerSpeedupMinute:60}`; `trees` = 3 arbres × recherches × niveaux (`req` = prérequis même arbre, `reqWA` = palier bâtiment requis). Note meta : « généré depuis `tools/data-src/war_academy.csv` » (CSV **non** commité). |
| `truegold_war_advanced_db.json` | **Objet** `{meta, categories, techs}` (700 Ko, 24 Ko gzippés). 92 techs de l'arbre **« War Academy Advanced »**, **1010 niveaux**, 4 catégories (`special` 9 / `economy` 2 / `capacity` 7 / `combat` 74). Tech : `{id, baseId, tier, tierRoman, name{EN,FR}, category, effect{EN,FR}, effectUnit, iconSlug, maxLevel, unlockWA, totals{…}, levels[]}`. Niveau : `{level, reqWA, req[{techId,level}], ttg, dust, gold, bread, wood, stone, iron, time, effectTotal, effectDelta, raw{}}`. **`time` en minutes** (comme `truegold_war_db.json`). **`reqWA`** = palier TG de l'Académie exigé (TG5→TG8) ; **`req`** = prérequis inter-techs (1000 niveaux sur 1010 en ont un). **`effectTotal`** = bonus **cumulé** atteint à ce niveau (la source l'affiche ainsi), `effectDelta` = gain du seul niveau ; unité donnée par `effectUnit` (`percent` 83 techs / `flat` 9). ⚠️ **Précision mixte** : `dust`, `ttg`, `time` sont **exacts** (et ce sont eux qui contraignent l'arbre) ; `gold/bread/wood/stone/iron` sont **arrondis** par la source (`17K`, `1.1M`) — `levels[].raw` garde le texte d'origine pour que l'imprécision reste visible. Pas de puissance par niveau (`totals.power` seulement). **FR volontairement à `null`** : la mise à jour n'est pas encore sortie sur le serveur de Paul (estimation ~50 j au 2026-08-09), les libellés officiels n'existent nulle part — **ne pas les inventer ni les traduire automatiquement**. **Provenance** : coûts par niveau récupérés d'un export xlsx de la page (le tableau déplié y était aplati en texte dans une cellule par tech) + instantané HTML pour noms/catégories/icônes/puissance ; `kingshotoptimizer.com/data/advanced-truegold-research` v1.13.0. Scripts d'extraction non commités. |
| `heroes_db.json` | **Liste** de 34 héros : `{id, name, generation, rarity, troopType, goodJoinerBear, GoodJoinerBearRank?, skills[], widget?}`. **`name` est une chaîne simple**, pas un objet `{EN,FR}` (les héros gardent leur nom en jeu) ; il sert aussi de nom de fichier du portrait (`img/heroes/<name>.webp`). `skills[]` = `{name{EN,FR}, effect{EN,FR}, levels[5]}`. **`widget` = Équipement Exclusif** — légendaires uniquement (22/34) : `{name{EN,FR}, effectConquest{name{EN,FR}, description{EN,FR}, levels[5]}, effectExpe{…}}`. Détails §9. |
| `beartrap_joiners_db.json` | **Objet** `{_meta, byGeneration}`. `byGeneration[gen]` = `{S:[ids], A:[…], B, C, D}` (rang du héros-joiner à cette génération de serveur, IDs = `heroes_db.json`). Cumulatif et sujet au power-creep (un même héros change de rang selon la gen). En cas de doublon d'id, le **meilleur** rang prime. Converti depuis une tier-list communautaire (xlsx non commité). |
| `masters_db.json` | **Liste** de 6 experts : `{id, name, title, affinityBonus, affinityMilestones[{level,affinity,emblems,bonus}], passive, skills[], affinity}`. **Convention `effect`** (passif & skills) : chaîne remplaçant le(s) « X » de la phrase. Une seule valeur → ex. `"+27%"`. **Deux valeurs** (phrase à deux X) → format `"(a;b)"` (ex. `"(5;20)"`) : la BDD l'éclate en deux colonnes « Effet 1 »/« Effet 2 » (cf. §3). |
| `shopcalc_items.json` | **Liste** de 91 objets : `{id, name{EN,FR}, img?, category, gemValue, skin?:true}` (référentiel de valeur). **Les accélérateurs restreints se relèvent comme les généraux** : un accélérateur d'entraînement (icône casque) est saisi en `1h_general_speedup` / `5m_general_speedup`, jamais en objet distinct. Choix de Paul, valable pour **toutes** les boutiques : une minute vaut une minute, et le référentiel reste court. Conséquence à connaître : une boutique peut alors porter deux lignes du même `itemId` au même palier avec des quantités différentes (Magasin du Théâtre : 30 et 36 accélérateurs 1h) — c'est normal, ce sont deux offres distinctes du jeu. **`img` (optionnel) découple le nom affiché du nom de fichier de l'icône** : `scImg()` prend `img` s'il existe, sinon `name.EN`. Sans lui, **corriger un nom anglais casse son icône en silence** — c'est ce qui a motivé les deux entrées actuelles (`governor_charm_material_chest`, `1000_hero_xp`), renommées côté EN alors que leur fichier gardait l'ancien nom. Renommer un `name.EN` sans vérifier `img/Item/` est donc un piège. Les entrées **`skin: true`** (4 à ce jour) sont des *variantes visuelles* référencées par `skinId` dans les boutiques/coffres : elles n'ont ni `category` ni `gemValue` (la valeur reste celle de l'objet porteur, ex. `town_skin`), et fournissent le nom affiché entre parenthèses + l'image de la carte. Un `skinId` qui ne correspond à aucun id **échoue en silence** (parenthèse absente, image de l'objet porteur) — vérifier le référencement croisé après ajout. |
| `shopcalc_classic.json` | **Liste** de boutiques : `{id, slug, name, resourceName, resourceShort, items[{itemId, qty, cost}]}`. |
| `shopcalc_events.json` | **Liste** de boutiques d'événement : `{id, slug, name, endsAt, resourceName, items…}`. **`items[].tier`** (optionnel, entier ≥ 1) = **palier de prix**. Certaines boutiques vendent le MÊME objet plusieurs fois à des prix croissants — repéré en jeu par la couleur de fond de la carte. Chaque palier est alors **une ligne à part entière** (même `itemId`, `cost` et `qtyMax` différents) : le tableau les affiche toutes, avec une colonne « Palier » triable, et le podium dédoublonne par `itemId` pour ne pas montrer trois fois le même objet. Sans `tier` la colonne n'apparaît pas — toutes les autres boutiques sont inchangées. Au Magasin du Théâtre : 9 offres × 3 paliers + 1 offre propre au palier 3 = 28 lignes, à +50 % puis +20 % environ d'un palier au suivant. `tier` est un champ **ADMIN** : il est réappliqué depuis le fichier à chaque chargement (`scLoadEvents`), donc un vieux `localStorage` ne peut pas l'effacer. |
| `shopcalc_chests.json` | **Liste** de coffres : `{id, slug, name, items[… , skinId?]}`. |
| `shopcalc_euro.json` | **Objet** `{_meta, packs, items}` — seconde valorisation, en argent réel, reconstruite depuis le relevé in-game des packs récurrents (`Pack_ks.xlsx`, fourni périodiquement par Paul ; cf. `CLAUDE.md`). **`_meta`** porte ce qui vaut pour TOUT le relevé : `tier` (palier de serveur, ex. `Gen 7 / TG5+`), `packPrice` / `packPriceUsd` (prix **par défaut** — 6 € / 5 $ — que partagent 60 des 63 packs relevés, donc il vit ici et n'est pas répété 60 fois ; les autres portent le leur, cf. `packs`), `updatedAt`, `source`, `note`. **`packs`** = dictionnaire `id -> {FR, EN, price?, priceUsd?}` des packs cités ; l'`id` est le slug du nom EN et sert aussi de **nom de fichier d'image** (`img/packs/<id>.webp`). **`price` / `priceUsd` sont OPTIONNELS mais indissociables** : absents, le pack coûte le tarif par défaut de `_meta` ; présents **tous les deux**, c'est LEUR valeur qui chiffre ses objets (`scEurPackPrice(pid)`). Un pack n'en déclarant qu'un seul retombe **entièrement** sur le défaut — faux aussi, mais cohérent entre € et $ donc repérable en basculant la devise, là où un demi-tarif serait faux du simple au double et silencieux. Trois packs hors défaut à ce jour : **Carte Mensuelle Ultra** et **Privilège de Recherche** (12 € / 10 $), **Ranger de la nature** (24 € / 20 $). **`items`** est indexé par l'`id` de `shopcalc_items.json` : `{qty, packs[]}` où `packs[]` liste les packs au **MEILLEUR PRIX UNITAIRE** (prix du pack ÷ quantité obtenue) et `qty` est la quantité qu'ils y donnent. **La règle disait « le plus d'exemplaires » tant que tous les packs coûtaient pareil** ; depuis les packs à 12 € elle se retourne — 20 accélérateurs 3h à 24 € reviennent plus cher l'unité que 12 à 6 €, et c'est bien le prix unitaire qui décide. **À prix unitaire ÉGAL entre deux packs de tarifs différents, c'est le MOINS CHER qui est retenu** — le format `{qty, packs[]}` ne sait pas décrire « 24 dans un pack à 6 € OU 96 dans un pack à 24 € », et à l'unité près c'est le petit ticket qui se conseille. Cas réel : le **Ranger de la nature** (24 €) est exactement 4× le pack **Rencontre Frontalière** (6 €), donc à égalité sur les Épices et l'Emblème — la page affiche le pack à 6 €. Le relevé les départage à la génération et `scEurRawPrice()` reprend le minimum, pour qu'un fichier mal régénéré ne chiffre pas l'objet au pack cher. **Un « Coffre Personnalisé » ne donne pas tout : il fait choisir 3 objets dans une liste** — et rien n'oblige à les prendre différents. Le relevé range donc ces objets-là à **`nb × 3`** (colonne *Type* de l'Excel : `Au Choix` contre `Inclus`), ce qui reste bien la quantité **maximale** obtenue dans ce pack, au prix d'y mettre ses trois choix. Cinq objets en dépendent — l'**Or** (25 000 × 3 = 75 000 sous la Sélection Personnalisé), les deux **Marques de Dressage**, le **Coffre d'Avancement Animal** et la **Caisse d'Équipement de Héros Chanceux** (celle-ci à égalité avec un pack « Inclus »). Ne pas « corriger » ces quantités vers `nb` : ce serait chiffrer un pack de choix comme un pack fixe. **Un seul pack** → son image illustre l'objet ; **plusieurs** → la page affiche « Multipack » et aucune image (le JSON les garde tous). Le prix unitaire n'est **jamais stocké** : c'est `prix du pack retenu / qty`, calculé à l'affichage par `scEurUnit()` — le stocker en ferait une seconde vérité, qui divergerait dès la moindre correction. Fichier **ADMIN en lecture seule** : aucune édition joueur, aucun `localStorage`, régénérable d'un bloc. **Couverture partielle assumée : 58 objets relevés sur les 87 valorisables** (64 chiffrés au total avec les calculés) — un objet non couvert vaut `null`, **jamais 0** (cf. règle du « — », §9). Les deux valorisations sont **indépendantes** : aucun taux de change gemme↔euro n'est calculé nulle part. **Trois blocs corrigent ensuite ce que le relevé seul dit mal.** Aucun ne vient de l'Excel : les **conserver** lors d'une régénération d'un bloc de `items` — c'est précisément leur raison d'être. Aucun ne stocke de valeur, seulement le lien ou le facteur ; tout se recalcule à l'affichage, dans l'ordre `derived` → `speedups` → `items`, puis `weights` sur le résultat (`scEurUnit()` / `scEurResolve()`). **`derived`** `id -> {fromId, factor | from:[{id, factor}], packs?, how{FR,EN}}` déduit un prix d'un ou **plusieurs** autres objets — `from[]` en SOMME les termes, `fromId`/`factor` reste la forme courte à une base —  et **l'emporte sur le relevé quand les deux existent** — un relevé peut être exact et son prix unitaire absurde. Une règle qui ne se résout pas (base absente, facteur invalide, cycle) rend « — » et **jamais** le relevé qu'elle devait corriger ; dans une somme, **un seul terme irrésolu annule tout** — une somme amputée d'un terme n'est pas un chiffre approché, c'est un chiffre faux. Chaque terme part avec sa PROPRE copie du chemin anti-cycle : deux termes peuvent légitimement partager une base (le Satin sous la caisse de variétés), et un chemin commun ferait passer le second pour un cycle. Treize entrées : la Caisse d'Équipement de Héros Mythique Personnalisée (aucun pack ne la vend ; elle donne à coup sûr ce que la Caisse Chanceuse donne 1 fois sur 100 → **× 100**, soit 16,67 € / 13,89 $) ; la **Marche Rapide 1** (**× 0,5** la Marche Rapide 2 — elle enlève 25 % du trajet contre 50 %, donc la moitié du travail ; le relevé la sortait à 1,50 € contre 1,00 €, l'inverse du bon sens) ; le **fragment ciblé de héros mythique** (**× 0,8** le fragment universel, qui va sur n'importe quel héros) ; et les **3 caisses de ressources personnalisées**, alignées sur le pain — Niv. Les quatre dernières sont des **barèmes d'EXP**, où le rapport entre deux formats de jeton est une égalité arithmétique et non une estimation. `1000_exp_vip` dérive du lot de 100 par un facteur **10** : 1 000 points d'EXP VIP, c'est exactement dix fois 100 — une égalité arithmétique, pas une estimation, et le référentiel gemmes applique déjà ce même ×10 (200 → 2 000). Sans elle les packs Épique et Mythique du Magasin du Théâtre, qui versent le lot de 1 000, restaient sans prix réel et sous-évaluaient le retour sur investissement. `10000_exp_vip` pousse le même raisonnement d'un cran (**× 100**, soit 20,00 €) ; côté héros, seul le lot de 10 000 est relevé, d'où `1000_hero_xp` (**× 0,1**) et `5000_hero_xp` (**× 0,5**) — de l'EXP de héros reste de l'EXP de héros quel que soit le format du jeton, et le référentiel gemmes applique déjà ces mêmes rapports (300 → 30 / 150). Ces trois-là viennent de l'onglet `Liste item` de l'Excel, où Paul range ces jetons en « Barême ». 1 = 1 × `10k_bread`, Niv. 2 = 10 × Niv. 1, Niv. 3 = 100 × Niv. 1, toutes trois sous le **Pack Lien Vital de la Ville** via `packs`. Ces trois-là sont **sorties de `weights`** : elles héritent du pain, pondération comprise, et les y laisser appliquerait le × 0,25 une seconde fois. Enfin **3 caisses du Chef, qui valent ce qu'elles RENDENT et non ce qu'un pack les vend** — le relevé les chiffrait au prix de la Carte d'Avantages qui les contient, ce qui n'a aucun rapport avec leur butin : la **Caisse de Matériaux de Talisman** (tirage 50/50 Guide/Plans, lots de 2 à 15 → **1,545 de chaque** en moyenne, 0,285 € contre 1,00 € au relevé) ; la **Caisse de Matériaux d'Équipement** (aucun hasard : 1 Vision + 4 Fils Dorés + 400 Satin → 0,284 €, presque le double du relevé) ; la **Caisse de Variétés d'Équipement** (Satin 1 fois sur 2, Fils Dorés et Vision 1 fois sur 4 → **469 / 2,345 / 1,9625** en moyenne, 0,358 € contre 3,00 € au relevé). Les quantités stockées sont des **espérances** (Σ lot × probabilité) calculées en amont : le fichier ne garde PAS la table de butin, seulement son résultat — la retrouver demande de relire le relevé de Paul. Aucune des trois ne nomme de pack : ce sont des calculs, la colonne « Pack d'origine » n'affiche donc que la pastille « Calculé », sans image. Leur ligne d'audit donne la somme **deux fois** — nommée (`469 × Satin + …`) puis en chiffres relevés (`469 × (6 € ÷ 26 000) + …`) : renvoyer le lecteur aux prix unitaires du tableau ne marche pas, ils y sont arrondis à 4 décimales et un facteur de 400 amplifie l'arrondi jusqu'à changer le total (400 × 0,0002 $ donne 0,080 contre 0,0769 réellement). Seules les divisions permettent de refaire le calcul à la main et de retomber sur le chiffre affiché. Le contrôle qui les valide : les deux caisses d'Équipement, objets cousins, tombent à 0,284 € et 0,358 € là où le relevé les séparait de 0,154 € à 3,00 € — un facteur 20 entre deux caisses jumelles. **`speedups`** `{basis{packs[], minutes, detail[]}, minutes{id->durée}, how}` fixe le prix d'**UNE minute** d'accélérateur = `prix du pack de base / basis.minutes` (`scEurBasisPrice()` — son prix à lui, pas le tarif par défaut : le jour où le pack le plus généreux sera un pack cher, la minute doit suivre), le pack le meilleur en minutes par euro servant de base (**Carte Hebdo Accélérer**, 6 €, 70×1h + 504×5min + 420×1min = **7 140 min** → 0,00084 € la minute), et chaque accélérateur vaut sa durée. Sans lui le prix suivait le format du jeton et non le temps : le 3h ressortait 6× plus cher à la minute que le 1h. C'est aussi ce qui chiffre l'**Accélérateur 8h**, qu'aucun pack ne vend. `basis` se relit dans l'Excel : le relevé `items` ne suffit pas, puisqu'il ne range un objet que sous le pack au meilleur prix unitaire (les 70 accélérateurs 1h de la Carte Hebdo sont invisibles dans `items`, où le maximum est 87). **`weights`** `[{factor, ids[], how}]` applique enfin une pondération **assumée** — un choix éditorial, pas une mesure : **× 0,25** sur bois, pain, pierre et fer, qui s'accumulent seuls en jouant et ne définissent pas un ROI (les caisses de ressources en héritent par `derived`, cf. ci-dessus). **L'or et les gemmes en sont exclus** : ce sont des monnaies, pas des ressources de construction. |
| `events/<slug>.json` | **Objet** `{_meta, missions[], freeDailyPack, waypost, stallMilestones[], explorationMilestones[], packs[]}` — le contenu d'un ÉVÉNEMENT, distinct de sa boutique (`shopcalc_events.json` reste la boutique elle-même). `_meta` : `{event, slug, days, tier, updatedAt, source, note}` — `event` est l'`id` de la boutique et sert de clé de persistance des achats (§8). Un **paquet de récompenses** (`missions[i]`, `freeDailyPack`, `waypost`, `packs[i].immediate` / `.daily` / `.reward`) a la même forme partout : `{coins?, stall?, travel?, items[]}`, où chaque item est soit `{itemId, qty}` (valorisable via le référentiel + le relevé €), soit `{label:{EN,FR}, qty}` (**listé, jamais chiffré** — règle du « — », §9). `eurExclude: true` garde la valeur gemmes mais sort l'objet du total € (relevé non représentatif : la Gold Key vaut à elle seule le prix du pack). Une mission `requiresPurchase` ne compte que les jours où un achat est coché. Un pack `once: true` donne `immediate` une fois puis `daily` jusqu'à la fin ; sinon **deux modèles de stock, à ne pas confondre** : `perDay` = plafond JOURNALIER (cellule = compteur cyclique) ; **`maxTotal`** = stock pour TOUTE la période, librement réparti (cellule = champ de saisie, plafonné au reste du stock — 2 le J1 et 5 le J4 sont un cas normal). Au Stand d'Aventure tous les packs sont journaliers ; à la Caravane du Dragon seul le pack « de Valeur » l'est, les cinq autres ont un stock de 20. **`doublesDailyMissions`** décrit un *pass* qui ne verse aucun lot quotidien propre mais **double les récompenses des missions** du jour de l'achat jusqu'à la fin (Minstrel Legends, « Double! Daily Mission rewards ») : `{coins, stall, travel, itemIds[]}` liste ce qui est effectivement doublé — pièces, Points de Vente, Points de Voyage, XP de héros et accélérateurs, mais ni les pièces d'amélioration ni les coffres de ressources. **`travelMilestones`** `{from, every, reward}` est une piste **répétée sans plafond** : une récompense à `from` points, puis une par tranche de `every` (4 500 / 5 000 / 5 500 …). À la différence de `stallMilestones` (liste finie de paliers), elle n'a pas de dernier cran — le nombre de crans se calcule, il ne se cherche pas dans une liste. Le **pack gratuit quotidien et la piste gratuite (`waypost`) ne sont pas des missions** : un pass ne les double pas. **`seasonalMissions[]`** = missions réclamées **une seule fois** pour tout l'événement, conditionnées par `minDays` (jours joués) et/ou `requiresPurchase` ; jamais doublées par un pass. **Piège de `requiresPurchase`** : en jeu, une mission « faire un achat » se valide avec **n'importe quel** achat, or plusieurs événements tournent en parallèle. D'où un contrôle dédié, **dont la forme dépend de la nature de la mission** — distinction essentielle, pas cosmétique : **saisonnière** (réclamée une fois) → une simple case « Mission « … » validée ? » (`purchaseOk`), étiquetée par le NOM de la mission ; **quotidienne** (un achat exigé CHAQUE jour) → un compteur « Jours d'achat ailleurs » (`outsideBuys`) qui s'ajoute aux jours cochés dans la grille. Une case unique sur une mission quotidienne la validerait sur TOUS les jours joués : c'est faux, et c'est le piège à éviter. Sans elle l'outil sous-estimait qui joue deux événements de front (relevé réel : 230 essences contre 195 calculées). **Tout est optionnel** : un événement sans `stallMilestones`, `travelMilestones` ni `waypost` (Caravane du Dragon) n'affiche simplement pas ces lignes. La monnaie **principale** n'est jamais nommée dans ce fichier — c'est le `resourceName` de la boutique, source unique ; les secondaires se nomment via `_meta.stallName` / `_meta.travelName`. Noms FR officiels en jeu : Minstrel Legends = « Légendes des Ménestrels », Waypost Sojourner = « Visiteur de Passage », Stall Points = « **Points de Vente** » (les libellés bilingues vivent dans le JSON). Prix **par pack** (`priceEur` / `priceUsd`), toujours renseigné — là où `shopcalc_euro.json` laisse le tarif par défaut de `_meta` s'appliquer aux packs qui n'en portent pas. **`short` {EN,FR}** = libellé court du pack pour les pastilles de la colonne « Provenance » du détail des gains (le nom complet y déborderait). **Provenance** : `Adventure_Stall.xlsx` (relevé in-game de Paul, non commité), **recalé sur une capture en jeu** — le tableur décrivait Minstrel Legends comme un lot quotidien fixe alors que c'est un pass à doublement, et son bloc n'avait aucune ligne « Stall Points ». Deux contrôles verrouillent le fichier (côté Caravane du Dragon, un troisième : 4 jours joués, aucun achat dans l'événement mais 1 achat ailleurs → **230 essences**, identique au stock en jeu de Paul) : un relevé réel (jour 3, pass acheté J1, 2 packs à 1,19 €) rend exactement **300 pièces / 200 points / 2 paliers**, le même relevé donne **7 200 Points de Voyage** — c'est LUI qui prouve que le pass double aussi cette monnaie (sans doublement on serait à 3 600, donc 0 cran au lieu de 6) —, et le F2P sur 5 jours rend **32 388 gemmes / 5,73 €** — soit les 30 588 du tableur **plus 1 800** apportés par la piste des Points de Voyage (200 gemmes + 2 accélérateurs 1h), que le tableur ignorait. Ne pas « corriger » ce total vers 30 588 : l'écart est la piste de voyage. Le total en **gemmes** est le contrôle stable ; sa contrepartie en **euros** suit le prix de la gemme et bouge donc à chaque relevé de packs — la revérifier après régénération de `shopcalc_euro.json` plutôt que de la croire acquise. À retenir pour tout relevé futur : **chaque source donne autant de Points de Vente que de Pièces d'Aventure** — un écart entre les deux totaux signale une ligne manquante. |

**`explorationMilestones[]`** (Magasin du Théâtre) = une piste de récompenses en **objets**, `{points, reward:{items[]}}` — à ne pas confondre avec `stallMilestones`, qui verse des *pièces*. Deux différences de fond : ses récompenses **se cumulent** (tout palier sous le total est acquis, il n'y en a pas qu'un seul d'actif), et son compteur n'est **pas calculé** — le joueur saisit lui-même le nombre d'Amulettes qu'il a DÉPENSÉES (`SE_PLAN.explore`, persisté avec le plan), parce que rien dans ses achats ne permet de le deviner. `_meta.exploreName {EN,FR}` nomme le champ de saisie.

**Tuile « Monnaie attendue » : conditionnelle.** `seHasCoins()` la masque quand aucune source du fichier ne verse de pièces. Au Magasin du Théâtre les packs donnent des Amulettes, qui alimentent un **tirage aléatoire** d'où sortent les Jetons Fantaisie : le montant dépend du hasard, l'outil ne peut pas le prédire, et afficher « 0 jeton attendu » laisserait croire que l'événement n'en rapporte aucun. `_meta.currencyNote {EN,FR}` porte alors l'explication affichée sous la section. Les événements qui versent bien des pièces (Stand d'Aventure, Caravane du Dragon) gardent la tuile inchangée.

**Amulettes Fantaisie : listées, jamais chiffrées.** Elles ne sont pas la monnaie de la boutique et leur rendement est aléatoire : elles vivent donc en `{label,qty}` (règle du « — », §9). Chaque pack contient aussi un petit coffre ×1, **cadeau d'alliance**, volontairement ABSENT du fichier — il ne se comptabilise jamais.

**`slug` (boutiques)** : c'est le nom de fichier de la page (`shop/<slug>.html`) et la clé de résolution de `scFindBySlug()`. Il a été **ajouté à côté de `id`, jamais à la place** : les éditions des joueurs sont persistées par `id` dans `shopcalcEvents`, renommer un `id` effacerait leurs données. Les `id` gardent donc leur casse d'origine (`Polar_Shop`, `Summit_Contest_Champion_store`) pendant que les `slug` sont en kebab-case.
| `changelog.json` | **Objet** `{_meta, releases[]}`, **la plus récente en premier**. Release : `{version, date "AAAA-MM-JJ", changes[]}` — **pas de titre de version** : l'en-tête ne porte que le numéro et la date. Un titre global mentait dès qu'une version contenait deux choses sans rapport (la v1.13 s'annonçait « Dis-moi ce qui cloche » et livrait aussi le Magasin du Théâtre). Change : `{type: "new"\|"improved"\|"fixed", title{EN,FR}, text{EN,FR}, href?, linkLabel?{EN,FR}}` — **chaque modification porte SON titre**, pour se lire seule. `title` est **optionnel** : les versions 0.x sont antérieures à ce format et s'affichent en description seule (le rendu ne pose pas de ligne vide). `href` = chemin relatif à la racine ; le libellé du lien vient de `linkLabel` s'il existe, sinon du nom de l'outil dans `site-config.js`, sinon « Ouvrir la page ». Plan de la page : **h1** (Nouveautés) → **h2** (la version) → **h3** (une modification) — la page est indexée, son plan doit se tenir seul. **NUMÉROTATION `MAJEURE.FONCTIONNALITÉ.CORRECTIF`** : le **2e** chiffre monte pour une FONCTIONNALITÉ (nouvel outil, nouvelle page, refonte d'une section, gros changement de structure), le **3e** pour tout le reste (une boutique de plus, une amélioration de confort, un correctif). Après la v1.13.1, une boutique ajoutée donne la v1.13.2 ; un nouvel outil, la v1.14.0. **`SITE.version` de `site-config.js` doit suivre** — c'est lui que le pied de page affiche. Les versions **0.x sont antérieures à la règle** et gardent leur numérotation d'origine. Les 10 premières versions (23/07 → 17/08/2026) ont été **reconstituées a posteriori** depuis les annonces Discord archivées dans l'historique git de `.github/news/announce.md`. ⚠️ **Les ancres viennent du numéro** (`v1-13-1`, généré par `announce.js`) : renuméroter une version publiée casse le lien des annonces Discord déjà postées. |
| `events/theater-shop.json` | Même schéma que `events/<slug>.json`, avec trois ajouts propres au Théâtre. **Deux échéances, à ne jamais confondre** : `_meta.endsAt` (07/09 00:00 UTC) ferme l'**ÉVÉNEMENT** — explorations et achat des packs —, tandis que le `endsAt` de la boutique dans `shopcalc_events.json` (08/09) ferme la **BOUTIQUE**, qui lui survit **24 h** pour laisser dépenser les Jetons. La grille d'achat, elle, se borne à `_meta.days` (6 colonnes = les 6 jours de l'événement) : c'est la même fenêtre, comptée en jours plutôt qu'en date. `_meta.endsAt` sert à l'échéance affichée par l'optimiseur (`stDeadline()`) **et à la fermeture du tirage** (`stEventOver()`), et `_meta.startsAt` (01/09) à numéroter les jours. **Pendant les 24 h qui séparent les deux dates, le verdict de l'optimiseur passe en tête sur « L'événement est terminé »** : le tirage clos, ni « Pousse » ni « Encaisse » ne sont jouables, et l'avertissement de `stDeadline()` s'efface au même instant — sans cette branche la page conseillait une exploration impossible. Les tableaux (chances, encaissement) restent affichés comme référence. Ne jamais borner un plan sur la fermeture de la boutique : elle inventerait une journée d'achat qui n'existe pas. **`_meta.startsAt`** (01/09) = jour 1, et `_meta.days` (6) la durée : ils numérotent les jours. **`missions[]`** : les Notes de Théâtre versent **20 amulettes en cumulé** sur trois missions gratuites (5 + 5 + 10, *pas* 20 chacune), plus 20 pour une mission `requiresPurchase` — un achat quelconque dans le jeu, event ou non. **`seasonalMissions[]`** porte le **cadeau d'ouverture** (10 amulettes, `minDays: 1`) : versé une seule fois, pas par jour. ⚠️ Il est **DÉDUIT, pas relevé** — au 3e jour le plan tombait 2 amulettes en dessous alors qu'il en restait 8 en jeu, un écart d'exactement 10. À confirmer en jeu ; la ligne de rapprochement de la page rend tout écart restant visible. Les amulettes y sont des `label` sans `itemId` : listées, jamais valorisées (elles ne sont pas la monnaie de la boutique). ⚠️ **Ces deux dates valent pour UNE édition** (01/09 → 07/09/2026) : à la suivante, il faut les reprendre, sans quoi la page annoncera zéro jour restant et le plan ne versera plus rien. |
| `events/fantasy-theater.json` | **Objet** `{_meta, pity, jump, fanstars, theater, fortress, rules}` — mécaniques du **TIRAGE**, à ne pas confondre avec `events/theater-shop.json` (packs, missions, paliers) ni avec la boutique `theater-shop` de `shopcalc_events.json` (dépense des Jetons). `pity.chances` = chance de monter à la n-ième tentative depuis la dernière montée (10/30/60/80/100 %, compteur remis à zéro à chaque montée) ; `jump` = de combien d'étages on monte quand ça passe (80 / 19,67 / 0,33 %) ; `fanstars.drops` = 1 à 5 Fanstars au sommet (72/14/8/4/2 %), 5 pour entrer en Forteresse. **`costByFloor` est indexé par l'étage de DÉPART** : le Théâtre s'arrête donc à 6 (l'étage 7 est le sommet, jamais un départ) et la Forteresse à 7 (elle compte 8 étages, confirmé en jeu). **`theater.tokensByFloor`** = relevé d'un serveur (0/140/300/850/1600/3000/6800) : le jeu ne publie ces montants nulle part et ils peuvent varier d'une génération à l'autre, d'où des valeurs **par défaut** modifiables dans la page. **`fortress.tokensByFloor` ne porte QUE le sommet** (13 000) : ses étages 1 à 7 reprennent ceux du Théâtre. **`rules.failTokenMultiplier` (10)** : une exploration **ratée** paie des Jetons — 10 fois son coût en amulettes (50 à l'étage 1, 1 000 à l'étage 6) —, une exploration réussie ne paie que l'étage atteint. Relevé en jeu par Paul le 04/09/2026 ; le guide n'en dit rien, et le moteur l'a ignoré pendant deux semaines. **Ce n'est pas un détail** : 64,69 % des amulettes partent sur des tentatives ratées (1,8324 échec pour 2,8324 tentatives par montée, le barème de garantie étant le même à tous les étages), donc le lot ajoute **6,47 jetons par amulette** — la page sous-estimait tout de ~53 %. Comme il vaut le même multiple partout, il **ne déplace pas** l'arbitrage pousser/encaisser (les seuils bougent de quelques amulettes, le meilleur étage où encaisser reste le 7) : il ne change QUE les montants. Le moteur le ramasse sur la seule branche d'échec (`ftFiniteValue`), et `ftClimbStats` en tient une table `cons[]` pour que « où encaisser » et la Forteresse le comptent aussi. Absent du fichier — un visiteur au JSON encore en cache, le site n'ayant pas de cache-busting —, il vaut 0 et le moteur retrouve exactement son calcul d'avant, sans erreur. **`theater.amuletLabel`** doit correspondre **exactement** au `label` des amulettes de `theater-shop.json` : c'est la clé sous laquelle `shop-event.js` range les objets non valorisables, et donc par elle que le module retrouve le total du plan. |
| `pets_db.json` | **Objet** `{_meta, pets[]}`. Chaque pet : `{id, name{EN,FR}, generation, maxLevel, skill{name{EN,FR}, desc{EN,FR}, cooldown?, effects[{label{EN,FR}, note{EN,FR}, values[]}]}, advancements[{growthManual, nutrientPotion, promotionMedallion}], petFood[]}`. 14 pets / 7 gén. **Conventions** : palier skill = **nb d'avancements faits** (caps aux niv. 10,20,…,`maxLevel` ; l'avancement au cap `N` débloque le palier `N/10` et **ne change pas le niveau** ; niveau `N` ≠ avancement fait → modèle à 2 statuts, cf. Masters) ; `values[]` = 1 valeur/palier (nb = `maxLevel/10` = nb d'`advancements`) ; `desc` garde le placeholder `X`/`X%` (remplacé au palier courant) ; `petFood[i]` = coût niv (i+1)→(i+2), longueur `maxLevel-1` ; le champ `note` existe mais **n'est plus affiché**. **Provenance** : converti depuis `Pets_data.xlsx` (non commité), FR relu à la main. |

**Provenance** : édition manuelle directe dans le JSON déployé. Aucune génération via GitHub Actions.

---

## 7. Automatisations (GitHub Actions)

Un seul workflow : **`discord-announce.yml`** (annonce Discord rédigée à la main, plus aucune traduction automatique).
- **Déclencheur** : push sur `main` touchant `.github/news/announce.md` (= committer ce fichier publie l'annonce) + `workflow_dispatch` avec entrée `dry_run` (aperçu sans envoi).
- **Mécanique** : `announce.js` lit `.github/news/announce.md`, extrait l'en-tête `<!-- kshub-news … -->` et les sections `## FR` / `## EN`, et POST un message à 2 embeds sur le `DISCORD_WEBHOOK` (`secrets`). Les commentaires HTML ne sont jamais publiés → gabarit vide = aucun envoi.
- **Pointeur** : la clé `covers-until` (SHA) de l'en-tête indique jusqu'où portait l'annonce précédente. Elle sert à lister les commits à couvrir au moment de rédiger la suivante. Aucun cache Actions, aucun commit de bot.
- **Clés d'en-tête** : **`version` (obligatoire)**, `covers-until`, `generated`, `ping` (mention, optionnel), **`ping-fr` / `ping-en`** (mention propre à chaque langue), `color` (hexa, défaut `F5B840`), `title-fr`, `title-en`.
- **Verrou changelog** : `version` doit désigner une entrée existante de `data/changelog.json`, sinon **`announce.js` refuse de publier** (code 1, message indiquant quoi corriger et les versions connues) — annonce Discord et page « Nouveautés » couvrent le même périmètre. `version: none` est l'échappatoire assumée pour une annonce qui ne touche pas au site. En aperçu (`dry_run`) le contrôle avertit sans bloquer, et un gabarit vide sort avant le contrôle (donc sans erreur). Quand la version est reconnue, le script ajoute **automatiquement** le lien `changelog.html#v<version>` en fin de chaque embed : ne pas l'écrire à la main.
- **Mentions** : la mention vit dans le `content` du **message**, jamais dans l'embed (une mention écrite dans un embed s'affiche mais **ne notifie personne**). Un message ne portant qu'un seul `content`, renseigner `ping-fr` et/ou `ping-en` **force l'envoi en deux messages** (FR puis EN), chacun avec sa mention. La clé `ping` seule garde l'ancien comportement (mention sur le premier message). Une mention de rôle ne notifie que sous la forme `<@&ID>` — en clair (`@MonRôle`) elle reste du texte mort.
- **Limites Discord** : 4096 car./embed, 6000 car. cumulés → au-delà de 5200 car. (FR+EN), le script scinde en deux messages au lieu de tronquer.
- **Convention** : committer `announce.md` **seul**. S'il part avec des modifs de site, celles-ci glissent simplement sur l'annonce suivante.
- **Secrets** : `secrets.DISCORD_WEBHOOK`. `vars.SRC_LANG` n'est plus utilisé.

---

## 8. Persistance (localStorage)

Clés « chrome » (hors registre, **globales — partagées entre profils**) : **`hub_lang`** (langue, défaut `EN`), **`hub_theme`** (thème, défaut `dark`), `help_seen_<id>` (bandeaux d'aide vus), **`kt_profiles`** (registre des profils), **`shop_view`** (lecture des boutiques : `eur` par défaut / `gem`) et **`shop_currency`** (devise de la lecture argent réel : **sans valeur, elle suit la langue** — `EUR` en FR, `USD` en EN — jusqu'au premier clic sur une pastille, qui écrit `EUR` ou `USD`). Ces deux dernières sont volontairement **hors `STORAGE_KEYS`** : ce sont des préférences d'affichage de même nature que le thème, pas des données de joueur — le relevé € lui-même n'est jamais écrit.

**Profils (comptes multiples)** — `js/profiles.js` (chargé tôt) installe un **proxy transparent sur `localStorage`** : les scripts continuent d'appeler `localStorage.getItem/setItem(STORAGE_KEYS.x)` sans modification, mais chaque **clé métier** (toute valeur de `STORAGE_KEYS`) est rangée sous `kt::<profileId>::<clé>` pour le profil actif. Les clés chrome ci-dessus restent globales. Au 1ᵉʳ chargement, les données « à plat » existantes sont **migrées** vers le 1ᵉʳ profil (aucune perte). API `window.Profiles` : `list/get/active/activeId`, `create/rename/remove/switch` (bascule = `location.reload()` + toast), `consumeSwitchToast`. Registre `kt_profiles` = `{v, activeId, mig, profiles:[{id, name, color}]}` — `mig` est la **version de migration des clés déjà appliquée** ; elle n'est posée que par une page qui charge `storage-keys.js`, ce qui empêche une page `database/*` de déclarer une migration qu'elle n'est pas en mesure de faire (cf. §9). UI dans `header.js` (pastille desktop + panneau, bloc drawer mobile, modale « Gérer les profils » réutilisant la DA `.backup-*`). L'export/import de `backup.js` opère automatiquement sur le profil actif (via le proxy) — pratique pour transférer un profil vers un autre appareil.

Clés métier — source unique **`js/storage-keys.js`** (`window.STORAGE_KEYS`) :
| Clé JS | Valeur localStorage |
|---|---|
| `caserneHeroes` | `caserne_user_heroes` |
| `caserneFilters` | `caserne_filters` |
| `masters` | `masters_user_data` |
| `researchDb` | `research_calc_db_v9` |
| `researchInputs` | `research_calc_inputs_v9` — porte aussi `bonusAsPercent: true`, marqueur d'unité du **Bonus de base** : il se saisissait en fraction (`0.753`), il se saisit désormais en **pourcentage** (`75.3`) comme sur TrueGold et l'Académie de Guerre. Sans le marqueur, `loadData()` convertit la valeur une fois (×100). Ne pas le retirer : un réglage relu sans lui serait multiplié une seconde fois |
| `beartrap` | `beartrap_data` |
| `beartrapJoiners` | `beartrap_joiners` |
| `truegold` | `tg_calc_data_v3` |
| `waracademy` | `wa_calc_data_v1` |
| `vikings` | `vikings_data` |
| `shopcalcItems` | `shopcalc_items` |
| `shopcalcClassic` | `shopcalc_classic` |
| `shopcalcEvents` | `shopcalc_events` |
| `shopcalcTab` | `shopcalc_tab` |
| `shopcalcCollapsed` | `shopcalc_collapsed` |
| `shopcalcEventPlans` | `shopcalc_event_plans` — achats cochés par événement : `{<id événement>: {played, buys:{<packId>:{<jour>:n}}, open}}`. Une seule clé pour **tous** les événements, indexée par l'`id` du fichier `data/events/*.json` |
| `pets` | `pets_levels` |
| `theaterOptimizer` | `theater_optimizer_data` — les trois saisies de l'optimiseur du Théâtre, `{floor, pity, tokens{1..8}, toksOpen}`. Rien d'autre : le plan d'achat, les jours joués et les amulettes dépensées vivent dans `shopcalcEventPlans`, chez `shop-event.js`. `tokens` porte le barème (8 lignes : les 7 étages du Théâtre, plus le sommet de la Forteresse) ; un état hérité de l'ancienne page autonome est **élagué au chargement** de tout ce que la grille tient désormais, sinon deux plans coexisteraient sans se voir |

Lecture sûre via `safeParse(key, fallback)` (try/catch → fallback si JSON corrompu). **Écriture** sûre via `window.ktSafeSet(key, value)` et `window.ktWarnUnsaved()`, tous deux dans `storage-keys.js` : un stockage plein ou interdit ne casse plus le rendu, le calcul continue en mémoire et une bannière prévient le joueur d'exporter avant de quitter. **Toujours les appeler par `window.` et sous condition** (`if (window.ktWarnUnsaved) …`) : sans cache-busting, une page neuve peut rencontrer un `storage-keys.js` en cache, et un nom global nu lèverait un `ReferenceError` (cf. §9).

Export/import ciblé par module dans `backup.js` (`BACKUP_MODULES`), en trois temps qui ne se mélangent pas : **l'export** lit chaque module pour lui-même (une valeur endommagée n'empêche plus le fichier de sortir ; les modules exclus sont nommés au joueur) ; **l'import** valide l'enveloppe *puis* le contenu de chaque module coché **avant la première écriture** — les quinze clés sauvegardées stockent toutes un objet ou un tableau, une chaîne ou un nombre est donc refusé —, et un échec d'écriture à mi-parcours **rétablit l'état antérieur en deux passes** (on efface tout ce qui vient d'être écrit, puis on réécrit les valeurs d'origine, qui retrouvent ainsi toute leur place). Le succès n'est annoncé qu'après écriture réussie.
**Partage de données inter-outils** : `beartrap.js` lit `caserneHeroes`; `vikings.js` réutilise `beartrap` (formations/troupes).
**Bonus de capacité liés à une compétence** (auto, mais toujours modifiables — même principe que le bonus PAN de TrueGold) :
- **Bonus Expert** (Bear Trap) ← compétence « Avantage primitif » (`savage_advantage`) de **Valora** : `masters_db.json` (valeur/niveau) + `masters` localStorage (niveau saisi page Experts).
- **Bonus Animal** (Bear Trap **et** Vikings) ← compétence du **Puissant Bison** (`mighty-bison`) : `pets_db.json` (valeur/palier) + `pets` localStorage (palier atteint page Familiers).
- Le champ se remplit tout seul (badge 🟢 Auto). Dès que l'utilisateur l'édite il passe en ✏️ Manuel ; le bouton ↺ resynchronise sur la compétence. Le mode auto/manuel est persisté (`cap-expert-auto`/`cap-animal-auto` dans `beartrap`, `animalAuto` dans `vikings`).

**Héros joiners autorisés (Bear Trap)** : le bouton « 🛡️ Héros autorisés » ouvre une modale (`#joiner-auth-modal`) listant les joiners de la **génération de serveur** sélectionnée, groupés par rang (`beartrap_joiners_db.json`). L'utilisateur coche ceux que son alliance permet ; **C et D décochés par défaut** + badge « Non recommandé ». Choix persistés **par génération** dans `beartrapJoiners`.
**Portée (important)** : la tier-list + l'autorisation ne s'appliquent qu'au **capitaine** de chaque marche *joiner* (slot 1, seul héros portant l'effet du rally) — capitaine choisi parmi les héros autorisés, base niveau+compétences puis **rang** (S→D) en départage. Les **renforts** (slots 2 & 3) n'influencent que la **capacité** de la marche (via leur niveau : `penalty = 13470 − capacité(niveau)`, cumulée sur les 3 héros) : on prend **n'importe quel héros débloqué**, **un par autre type** (composition imposée inf/cav/arc), au plus haut niveau, en **réservant** les capitaines potentiels (autorisés) pour les autres marches. Le mode **Hôte/organisateur** garde son ancienne logique (`organizerTierList`).

---

### Clés « chrome » du formulaire de retour

`fb_last` (horodatage du dernier envoi réussi), `fb_day` (`AAAA-MM-JJ|n`, compteur du jour) et `fb_cid` (identifiant anonyme du navigateur, tiré au hasard) sont **hors de `STORAGE_KEYS`**, volontairement — même nature que `shop_view` / `hub_theme`. Les y mettre serait un contresens à double titre : `profiles.js` isole toute valeur du registre par profil (changer de profil remettrait le garde-fou anti-spam à zéro) et `backup.js` en ferait une donnée exportable, alors que ce n'est pas une donnée de joueur. Elles doivent rester attachées au **navigateur**, pas au profil.

## 9. Conventions & pièges connus

**Conventions**
- **Manifeste unique** : ajouter une catégorie / un outil = éditer **uniquement site-config.js**. SITE.nameetSITE.home alimentent le logo du header. (jamais coder la nav en dur).
- **Nouvelle page = ne pas oublier `js/footer.js`** en dernière balise `<script>` (et une ligne dans `sitemap.xml`). Seule `pets.html` en est volontairement dépourvue. **Ne pas oublier non plus le bloc drawer figé** (cf. « maillage sans JavaScript » plus bas) : le recopier depuis n'importe quelle page existante, juste **avant** `<script src="js/site-config.js">`.
- **Fichiers de données : cache revalidé, jamais servi tel quel.** GitHub Pages sert `data/*.json` avec un cache de 10 minutes. Le HTML d'une page NEUVE, lui, n'est jamais en cache : un visiteur revenu dans cette fenêtre récupérait donc la page neuve avec l'ANCIENNE liste de boutiques, et lisait « Shop introuvable » sur une page pourtant en ligne — panne observée à la mise en ligne du Magasin du Théâtre, et reproduite à l'identique (barre des boutiques vide, vignette vide, titre resté anglais : tout découle de la boutique introuvable, `SP.kind` restant indéfini). `shop-core.js` passe donc ses 5 `fetch` par `scFetchData()`, qui ajoute `cache:'no-cache'` : ce n'est PAS une désactivation du cache mais une **revalidation** — 304 de quelques octets tant que le fichier n'a pas bougé, contenu neuf dès qu'il bouge. **Tout nouveau `fetch` de données doit passer par là** ; les autres fichiers (`heroes_db`, `pets_db`, `changelog`…) ont encore le comportement par défaut, moins visible faute de message d'erreur, mais tout aussi périmable.
- **Fichiers JS : jamais de nouveau nom global appelé d'un AUTRE fichier.** Les `<script>` du site n'ont aucun cache-busting, et le navigateur cache chaque fichier séparément : un visiteur de retour peut très bien charger le nouveau `shop-items-euro.js` avec l'ancien `shop-core.js`. Si la page appelle alors une fonction ajoutée en même temps qu'elle dans le core, c'est un **`ReferenceError` en plein rendu** — le tableau, déjà écrit, reste à l'écran avec les ANCIENS chiffres, pendant que tout ce qui suit disparaît en silence : encadré des règles, et surtout le branchement de l'aperçu au survol, qui ne se fait jamais. Panne observée à la mise en ligne des prix par pack (gemme à 0,0004 $ au lieu de 0,0007 $, aperçus morts), invisible en local puisque rien n'y est en cache. **Règle** : une page n'appelle que des noms globaux qui existaient DÉJÀ dans la version précédente du core ; un nouveau paramètre sur une fonction existante passe sans risque (le vieux code l'ignore et retombe sur son défaut — faux mais affiché, et corrigé au rechargement suivant), un nouveau NOM ne passe pas. D'où `iePackPriceOf()` / `ieBasisPrice()` dans `shop-items-euro.js`, qui enveloppent `scEurPackPrice(pid)` plutôt que d'appeler directement les `scEurRawPrice()` / `scEurBasisPrice()` nés avec eux. Les fonctions neuves du core restent libres de s'appeler entre elles : elles arrivent dans le même fichier, donc toujours ensemble.
- **Adresses sans `.html`, partout.** GitHub Pages sert `foo.html` aussi bien sous `/foo` que sous `/foo.html` : la même page a donc deux adresses, et c'est le `canonical` qui décide laquelle est indexée. La forme retenue pour tout le site est **la courte**. Concrètement : `X.html` → `/X`, et **`dir/index.html` → `/dir/`** (avec la barre finale — c'est l'adresse d'un dossier, pas `/dir/index`). Doivent porter la forme courte **ensemble**, sinon ils se contredisent et Google suit le `canonical` : `canonical`, les `hreflang`, `og:url`, le fil d'Ariane JSON-LD, le `sitemap.xml`, `llms.txt`, les `href` de `data/changelog.json`, **et tous les liens internes** — ceux écrits en dur dans les pages comme ceux fabriqués par le JS (`site-config.js`, `scShopHref()` dans `shop-core.js`, `hub.js`, le drawer de `header.js`, `HDR_ACTIVE_HREF`). **Deux exceptions**, et deux seulement : `404.html` (page spéciale de GitHub Pages, servie à n'importe quelle profondeur) et `google55512fe842dbeeaf.html` (jeton de vérification, dont le nom EST la preuve). Les FICHIERS, eux, gardent leur `.html` sur le disque : seules les adresses changent. **Test local** : un simple `python3 -m http.server` ne résout PLUS les liens internes, puisqu'il ne connaît que les noms de fichiers — servir le repo avec un serveur qui essaie `X.html` quand `/X` est demandé, comme le fait GitHub Pages, sinon toute la navigation paraît cassée en local alors qu'elle est bonne en ligne.
- **`<base href>` relatif, jamais absolu** : les pages en sous-dossier déclarent `<base href="../">` (1 niveau, `shop/*`) ou `<base href="../../">` (2 niveaux, `database/*/*`), selon leur profondeur. Le site est ainsi **insensible à son chemin de déploiement** : il fonctionne à la racine du domaine, sous `/Kingshot_Toolbox/`, et sur un serveur local à n'importe quel préfixe. Ne jamais revenir à un chemin absolu (`/Kingshot_Toolbox/`) : c'est ce qui rendait tout changement d'URL risqué.
- **Lien vers l'accueil = `./`, jamais `index.html`** (fils d'ariane, logo du header via `SITE.home`). Les deux servent la même page, mais la **canonique est la racine** : viser `index.html` envoyait 100 % du maillage interne sur l'URL qu'on demande à Google d'ignorer (SEMrush mesurait un ILR de 100 sur `/index.html` contre 9 sur `/`) et dédoublait l'accueil dans Analytics (`/` et `/index.html` comptés séparément). `./` se résout via le `<base href>` de chaque profondeur : la règle d'insensibilité au chemin de déploiement est préservée. Les `index.html` de sous-dossier (`database/*/index.html`) ne sont **pas** concernés : leur canonique porte déjà `index.html`, les trois listes (lien, canonique, sitemap) y sont cohérentes.
- **Google Analytics (GA4, `G-M1QG4XSD3K`)** : extrait officiel `gtag.js` collé **verbatim** dans le `<head>` de chaque page, juste après le script de thème (qui doit rester premier). Présent sur les 69 pages HTML, `404.html` comprise ; **seule exception `google55512fe842dbeeaf.html`**, le jeton de vérification Search Console. L'identifiant est donc dupliqué 69 fois : le changer = un remplacement global. Vérifié : le site se rend normalement quand le script est bloqué (bloqueur de pub).
- **Thème appliqué dans le `<head>`, avant le 1er rendu** : un `<script>` inline juste après `<meta charset>` lit `hub_theme` et pose `data-theme` sur `<html>`. Sans lui, la page s'affiche avec le `data-theme="dark"` écrit en dur, puis bascule quand `header.js` se charge **en fin de `<body>`** — et comme `body` porte `transition: background-color 0.3s`, le flash sombre est *animé*, très visible en navigation mobile. Mesuré : fond encore en transition à 150 ms avant correctif, clair dès 60 ms après. Toute nouvelle page doit porter ce script.
- **Le mode d'emploi d'une page se complète, il ne se réécrit pas.** `shop-page.js` pose UNE aide pour les onze boutiques ; celle du Théâtre a huit points de plus que les autres n'ont pas. Plutôt que d'écrire dans le fichier partagé des étapes qui ne parlent qu'à une page, il construit sa configuration puis appelle `window.spHelpExtras(cfg)` si un module l'a posée — `js/shop-theater.js` y ajoute son chapitre (`cfg.sections`), et la modale de `help.js` rend chaque chapitre sous son propre titre, numéroté à part. Absente, la fonction ne change rien : les dix autres boutiques gardent exactement l'aide d'avant. **Poser le hook au premier niveau du fichier, jamais après un `await`** : `shop-page.js` construit son aide après le chargement des données, mais un module qui attendrait de son côté passerait après lui.
- **Un greffon qui recopie les règles de `seCompute` dérive, toujours.** L'optimiseur du Théâtre recalculait de son côté ce que le plan avait déjà versé, à partir de `plan.buys` et des missions du fichier. Deux règles y manquaient aussitôt : les **achats faits ailleurs** (`outsideBuys`, que ce même lot venait pourtant d'activer sur cette page en ajoutant une mission `requiresPurchase`) — 60 amulettes de trop dans le solde « en poche », le seul chiffre que la page déclare comparable au jeu —, et les **lignes décochées** dans le détail des gains, qui faisaient accuser le fichier de données d'une omission inexistante. Le calcul borné (`seCompute(upTo)`) supprime la duplication : une seule source de vérité, et les règles futures de `seCompute` (packs `once`, pass qui doublent les missions, `freeDailyPack`, `waypost`) suivront toutes seules. **Décocher une ligne du détail** reste un cas à traiter à part : les revenus tombent à zéro pendant que les dépenses restent, et le solde devient légitimement négatif — le dire, plutôt que crier à la donnée manquante.
- **« Ne pas encaisser » ne veut pas dire « pousser ».** Une politique optimale qui renvoie « pas d'encaissement » le fait aussi bien parce que pousser est meilleur que parce qu'**aucune action n'est possible** — budget insuffisant pour une exploration, ce qui est l'état normal de fin d'événement. Le verdict conseillait alors une exploration impayable. Toute lecture d'un `argmax` doit distinguer « l'autre branche gagne » de « aucune branche n'est jouable ».
- **Un état relu du `localStorage` indexe des tables : le borner avant, pas après.** `floor` et `pity` entrent directement dans les tableaux typés du moteur. Un état hors bornes — reliquat d'une version précédente, ou sauvegarde importée depuis un autre appareil, ce que la Sauvegarde Globale rend possible — lisait au-delà du `Float64Array` et affichait des « NaN% ». Le bornage se fait au chargement, avant tout calcul.
- **Un plan d'événement projette l'avenir : il ne se compare pas au jeu.** La grille d'achat couvre TOUS les jours, y compris ceux où les packs ne sont pas encore achetables. Le total qu'elle verse est donc « ce que l'événement rapportera en tout », pas « ce que j'ai ». L'optimiseur du Théâtre affirmait qu'un écart entre son total et le jeu prouvait une erreur de données — c'était faux et systématique dès le 2ᵉ jour. Il distingue désormais **en poche aujourd'hui** (`total − dépensé − à venir`, le seul chiffre comparable au jeu, `stFutureIncome()` bornant l'avenir au jour d'événement en cours) et **à dépenser d'ici la fin** (le budget stratégique). Toute page greffée qui lit `c` d'un plan doit se poser la question : ce chiffre décrit-il le présent ou la projection ?
- **Un stepper ne sait pas ce qui s'est passé en jeu.** Reporter automatiquement le coût d'une exploration sur le compteur d'amulettes dépensées est tentant : +1 échec à l'étage 3, +20 dépensées. Mais une montée vaut **+1, +2 ou +3 étages pour une seule exploration payée** — facturer chaque clic du stepper d'étage compterait deux ou trois explorations là où le jeu n'en fait payer qu'une, et le compteur dériverait dans l'autre sens. D'où des **boutons d'action** (`stAct()`) qui disent ce qui vient d'arriver — *échouer*, *monter +1 / +2 / +3*, *encaisser* — le joueur choisissant lui-même la hauteur du saut, une seule exploration étant facturée quelle qu'elle soit. Les steppers restent à côté une correction libre et gratuite : séparer « j'ai joué » de « je corrige ma saisie » est ce qui rend le report fiable.
- **Un seuil de décision dépend de l'état, l'afficher comme une règle rend la page incohérente.** L'optimiseur annonçait « en dessous de 520 amulettes, encaisse » à côté d'un conseil « pousse » avec 388 — deux phrases qui se contredisent en apparence. Le seuil affiché était celui de l'étage 2 sur un compteur neuf ; à l'étage 3 avec 2 échecs il vaut **20**. Le compteur de garantie écrase tout : à un même étage, deux échecs au compteur peuvent renverser le conseil à budget égal, la montée suivante étant presque acquise. `ftAdviceRange()` prend donc l'étage ET le compteur en argument, et la phrase nomme l'état auquel elle s'applique.
- **Ce seuil n'est pas monotone en budget : ce n'est pas un seuil, c'est une plage.** On a longtemps cru qu'en dessous d'un budget on encaisse et qu'au-dessus on pousse. C'est faux : la décision bascule **245 fois** entre 1 et 4 000 amulettes à l'étage 2 sur un compteur neuf (13 fois à l'étage 4), parce que ce qui compte n'est pas seulement d'avoir de quoi viser le sommet, mais ce qu'on fera du reliquat — et le reliquat oscille. Deux bugs en sont sortis, tous deux vus à l'écran : (1) une **dichotomie** suppose UNE bascule et rend celle d'une autre plage — à l'étage 5 avec 248 amulettes, la page conseillait « Encaisse » puis annonçait une bascule à 242 au-dessus de laquelle il fallait pousser, se contredisant à une ligne d'intervalle ; (2) même juste, **un seuil seul se lit comme une règle**, or à l'étage 2 avec 20 amulettes pousser ne vaut mieux qu'à 24 *exactement*, et encaisser reprend de 25 à 28 — la moitié « au-dessus, pousse » de la phrase était fausse dans 4,5 % des états. `ftAdviceRange()` rend donc les **deux bornes de la plage continue** qui contient le budget du joueur, et la phrase n'affirme plus que ce qui s'y passe (« encaisser reste le bon geste jusqu'à X ; à partir de X+1, pousser redevient payant »). Un test balaie chaque état et chaque budget jusqu'à 600 pour vérifier les trois affirmations : conseil = verdict, le conseil tient à la borne, il change juste au-delà. **Ne jamais redonner un chiffre unique ici**, et ne pas remettre de dichotomie : la fonction rend la plage parce que la fonction sous-jacente n'est pas monotone.
- **Une table de décisions se résout UNE fois pour tous les budgets.** Résoudre pour B calcule déjà tous les budgets plus petits : `ftSolveValue()` (extrait de `ftFiniteValue`) rend la table entière et la mémoïse par (zone, barème), si bien que le verdict et la plage se lisent dans la MÊME table. Avant, lire une plage relançait onze résolutions complètes (dichotomie) et le verdict une douzième — 44 ms chacune au plafond, sur un budget de 12 000 amulettes 131 ms. Le cache ne se vide qu'à une correction du barème de jetons par le joueur.
- **Un budget qu'on plafonne à zéro cache le bug qui l'a rendu négatif.** L'optimiseur du Théâtre déduisait les amulettes restantes du plan (`total − dépensées`) et écrasait un résultat négatif par `Math.max(0, …)`. Or un reste négatif est **impossible en jeu** : il ne veut pas dire « zéro », il prouve que le modèle ignore une source d'amulettes. Le plafond a masqué le cadeau d'ouverture de l'événement pendant deux jours — la page affichait 0 là où il en restait 8, et l'écart de 10 ne se voyait nulle part. La section porte désormais une **ligne de rapprochement** (`stReconcileHtml`) qui écrit le calcul en toutes lettres et qui est la seule de la page à se comparer directement au jeu ; un reste négatif s'y annonce comme une anomalie de données, pas comme un solde. Règle générale : ne jamais borner une valeur dérivée dont le dépassement est un diagnostic.
- **`style.css` raye TOUTES les tables : une classe seule ne surligne pas une ligne.** `tbody tr:nth-child(even) { background-color: var(--table-row-alt) }` est une règle globale de spécificité (0,1,2) — un `.ma-classe` posé sur un `<tr>` (0,1,0) perd contre elle une ligne sur deux. La mise en avant « Meilleure » du tableau d'encaissement est ainsi restée invisible : elle tombait sur une ligne paire. Qualifier comme le fait déjà le survol du même fichier — `.sxe-grid tbody tr.ma-classe` (0,2,2) l'emporte. **Et doubler la règle sur `td.c-pack`** : cette colonne est `position: sticky` avec un fond OPAQUE, aucun fond de ligne ne passe dessous, sinon la bande démarre une colonne trop loin.
- **Ne jamais greffer un bloc essentiel dans un `<details>` dont l'état est enregistré.** La section des amulettes était ajoutée à la fin du panneau « Mes achats » : un clic sur son résumé la faisait disparaître, et l'état plié étant persisté (`shopcalc_event_plans`), elle ne revenait pas au rechargement — alors que l'intro de la page et le mode d'emploi la promettent tous les deux. Elle vit maintenant à la fin de la SECTION, après le panneau et sa note. Même position de lecture, sans l'interrupteur.
- **Un `null` qui a deux sens opposés ne se remplace pas par `|| 0`.** `ftAdviceRange` rendait `null` aussi bien quand pousser vaut TOUJOURS mieux que quand encaisser vaut toujours mieux. Un `|| 0` dans la phrase du seuil transformait le second cas en « pousser paie dès 0 amulette » — l'inverse exact du conseil. Une valeur absente se teste, elle ne se replie pas sur un chiffre qui, lui, veut dire quelque chose.
- **Une pastille `.sx-fact` en `nowrap` peut faire défiler toute la page.** `.sx-fact` porte `white-space: nowrap` — parfait dans l'en-tête de boutique, où les faits sont courts (« Fin dans 5 j »). Dans `.sxe-toolbar`, le plus long (« Amulettes Fantaisie dépensées » et son compteur de paliers) dépasse à lui seul la largeur d'un téléphone ; comme un élément flex ne rétrécit jamais sous son contenu (`min-width: auto`), le `flex-wrap: wrap` du conteneur n'y peut rien et c'est la PAGE qui s'élargit. Mesuré : **52 px** de débordement horizontal sur le Magasin du Théâtre, **18 px** sur le Stand d'Aventure, 0 sur la Caravane du Dragon (ses libellés sont plus courts) — d'où un bug qui paraissait propre à une page alors qu'il tenait à la longueur d'un mot. Corrigé par `.sxe-toolbar .sx-fact { white-space: normal; flex-wrap: wrap; max-width: 100% }` : la pastille se replie au lieu de pousser la page, sans rien changer sur grand écran (barre d'outils mesurée identique au pixel, 1178 × 84). **Le symptôme trompe** : le premier élément trop large qu'on trouve est `#hdr-drawer-overlay`, en `position: fixed; width: 100%` — il ne déborde pas, il suit la largeur du document déjà élargi. Chercher la cause en masquant les enfants de `<body>` un par un, jamais en regardant qui est le plus large.
- **Une règle déduite qui passe au-dessus du prix du marché est un signal, pas un détail.** Depuis le pack **Privilège de Recherche** (160 caisses de ressources Lv3 pour 12 €, soit **0,075 €** l'unité), le relevé chiffre cette caisse à moins de la moitié de ce qu'en dit la règle déduite : **0,160 €**, soit 100 × la caisse Lv1, elle-même alignée sur le pain pondéré ×0,25. `derived` l'emportant sur le relevé, c'est 0,160 € qui s'affiche. L'un des deux est faux — le facteur ×100 ou l'alignement de la Lv1 — et seul le jeu peut trancher : ouvrir une Lv1 et une Lv3, comparer les quantités de ressources. **Non tranché** ; laissé tel quel plutôt que corrigé au jugé, parce que toucher au facteur déplacerait d'un coup les trois caisses sur toutes les boutiques.
- **`localStorage` = par origine, pas par domaine logique** : un changement de nom de domaine rend **toutes** les données utilisateur invisibles (héros, recherches, profils, thème, langue). Elles ne sont pas effacées, elles restent sur l'ancienne origine. C'est ce qui s'est produit au passage à `kingshottoolbox.com`. La page de reprise `migrate.html` (export/import du `localStorage` d'une origine à l'autre) a rempli son rôle et a été **supprimée du dépôt** une fois la bascule digérée ; la retrouver dans l'historique git si un jour le cas se représente. **À anticiper impérativement avant tout futur changement d'adresse.*
- **Safari efface le `localStorage` après 7 jours** d'utilisation du navigateur sans interaction avec le site (ITP, aussi sur iOS/iPad). Deux conséquences : les données laissées sur une ancienne origine ont une **date de péremption d'environ une semaine**, et sur iPhone il n'existe **aucune console** — la seule reprise possible passe par une page servie depuis l'ancienne origine (dépôt `aistra91-ks.github.io`). Toute future page de reprise doit donc offrir un export **par presse-papier** et un import **par collage de texte**, Safari bloquant parfois les téléchargements déclenchés par script — c'est ce que faisait `migrate.html`.
- **`localStorage` peut être REFUSÉ, pas seulement plein.** Cookies bloqués ou mode strict : c'est l'**accès à la propriété** `window.localStorage` qui lève, avant toute lecture. `profiles.js` mourait alors sur sa première ligne, `window.Profiles` n'existait jamais et le bandeau d'avertissement n'était pas posé non plus. Il sonde désormais le stockage à l'amorçage et, s'il est indisponible, expose une **API inerte** (un profil fictif, aucune écriture) pour que header et sauvegarde continuent de tourner, puis prévient le joueur. **Limite connue et assumée** : une quinzaine de lectures `localStorage.getItem` non protégées subsistent dans les modules de page (aide, thème, restauration d'état) et lèvent encore dans ce mode — les pages rendent et avertissent, mais pas sans erreur console. Le cas *stockage plein*, lui, est entièrement couvert.
- **Piège de `profiles.js` — CORRIGÉ.** Le registre `kt_profiles` était écrit dès le premier chargement, y compris sur une page `database/*` qui ne charge **pas** `storage-keys.js` : `BUSINESS_KEYS` y est vide, la reprise des clés « à plat » ne se faisait donc pas, et la seule présence du registre marquait pourtant la migration comme faite — définitivement, puisqu'elle ne repassait jamais. Reproduit : arriver par `/database/buildings/` puis ouvrir `/caserne` rendait la Caserne vide alors que la clé à plat était toujours là. **Création du profil et migration des clés sont désormais deux opérations distinctes** : le registre porte `mig` (version de migration appliquée), que **seule** une page connaissant `STORAGE_KEYS` peut poser. La migration est donc idempotente et reprenable, elle n'écrase jamais une valeur déjà présente dans le profil, et elle ne retire une clé à plat qu'après l'avoir recopiée. Effet de bord voulu : un joueur déjà touché **récupère ses données** au prochain passage sur une page d'outil. La portée réelle restait étroite (le passage au domaine propre avait déjà vidé les clés à plat, cf. ci-dessus) — mais le mécanisme, lui, était bien cassé.
- **SEO du `<head>` : bloc standard sur chaque page**, dans cet ordre : `charset` → `viewport` → `theme-color #0a0a0a` → `title` → `meta description` → `canonical` → **Open Graph + `twitter:card`** (og:title = title sans le suffixe « | Kingshot Toolbox », og:description = la description, og:url = la canonique, og:image = `img/logo/og-image.png` en absolu) → favicons → CSS. Toute **nouvelle page** reproduit ce bloc **et** ajoute sa ligne `sitemap.xml`.
- **Tirets des titres/descriptions : « - » simple, jamais « — » cadratin** (préférence de Paul pour l'affichage Google). Le cadratin reste toléré dans le corps des pages.
- **JSON-LD** : `WebSite` + `Organization` sur l'accueil uniquement ; `WebApplication` sur les 8 pages outils ; `BreadcrumbList` sur toute page qui affiche un fil d'ariane `.db-breadcrumb` (le balisage doit refléter le fil visible — libellés EN), **plus les 8 pages outils** qui n'en affichent pas : leur fil à 2 maillons (`Kingshot Toolbox` → nom de l'outil) reprend la hiérarchie que montre le header injecté (logo → outil actif). Un maillon intermédiaire exige une URL côté Google : les catégories du manifeste n'en ayant pas, on s'en tient à 2 maillons. Le dernier maillon doit toujours valoir **exactement** la canonique de la page. Blocs compacts en fin de `<head>`, à valider avec `json.loads` avant commit.
- **Un seul `<h1>` par page** : pages BDD/boutiques = le nom visible ; Caserne/Experts = le titre de page (promu, taille figée en inline) ; les 5 outils sans titre visible portent un `h1` masqué accessible (texte = nom de l'outil, jamais du bourrage de mots-clés).
- **Le maillage interne doit exister sans JavaScript.** Le header et le pied de page sont injectés par `header.js`/`footer.js` : un lien qui n'existe que là est invisible pour un robot qui n'exécute pas le JS, et la page vers laquelle il pointe se retrouve orpheline. D'où **la grille du hub écrite en dur dans `index.html`** (13 liens), au même titre que les cartes de `shop_calc.html` et les `db-switch` des pages `database/*`. `hub.js` re-rend ensuite le même bloc depuis `SITE` : la **source de vérité reste `js/site-config.js`**, et l'ajout d'un outil se répercute dans `index.html` en recopiant le rendu obtenu **en anglais**. Conserver aussi `.hub-intro` (clé i18n `hubIntro` dans `js/hub.js`), seul texte suivi du hub.
  **Le drawer suit désormais la même règle** : le bloc `#hdr-drawer-overlay` + `#hdr-drawer` (avec sa `nav.drawer-nav` et les 13 liens outils/BDD, libellés EN) est **figé en dur dans les 65 pages qui chargent `header.js`**, placé **avant** les `<script>` de fin de page. L'ordre est impératif : `header.js` ne crée le drawer que s'il ne le trouve pas (`if (!document.getElementById('hdr-drawer'))`), et ce test s'exécute avant que le parseur n'ait atteint un bloc placé plus bas — d'où **deux drawers** superposés si on l'insère après les scripts (erreur commise puis corrigée). `hdrBuildDrawer()` remplace ensuite le contenu par sa version complète (icônes, profil, langue, thème) : le DOM final est identique au commentaire HTML près.
  Gain : les 8 outils passaient pour des **culs-de-sac** en HTML brut (1 lien entrant, **0 sortant**) ; ils comptent maintenant 64-65 pages entrantes. Ça ne change rien pour Google, qui exécute le JS et voyait déjà tout — mais **les crawlers d'IA (GPTBot, OAI-SearchBot, ChatGPT-User) ne l'exécutent pas**, et c'est aussi ce qui explique l'ILR de 18 mesuré par SEMrush sur les outils : un audit sans rendu JS. Signe qui ne trompe pas, cet audit n'avait pas trouvé `about.html` ni `changelog.html`, liées uniquement depuis le pied de page injecté.
  **Piège mesuré** : `.hdr-drawer` porte `box-shadow: -12px 0 40px rgba(0,0,0,0.5)`. Même à `translateX(100%)` (hors écran), l'ombre déborde sur les ~72 px du bord droit — imperceptible mais réel (delta 4/255), et visible **uniquement sans JavaScript**, où le drawer n'existait pas avant. D'où `.hdr-drawer-raw { box-shadow: none; }`, portée par le bloc figé et retirée par `hdrBuildDrawer()` à l'hydratation. Vérifié au pixel : rendu strictement identique avec **et** sans JS, desktop et mobile.
- **`<link rel="canonical">` absolu sur chaque page** : `https://kingshottoolbox.com/<chemin>`, l'accueil pointant sur la racine (sans `index.html`). C'est la **seule URL absolue** du HTML — tout le reste reste relatif. Toute nouvelle page en porte une, et son URL doit correspondre **exactement** à sa ligne de `sitemap.xml` : les deux listes se vérifient en 1:1 (65 = 65 aujourd'hui).
- **URL du sitemap = celle du site publié** : `https://kingshottoolbox.com/sitemap.xml`. Ne jamais donner à un validateur l'URL GitHub `.../blob/main/sitemap.xml` : c'est la **visionneuse de fichiers** de github.com, servie en `text/html`, d'où l'erreur *« Incorrect http header content-type: text/html (expected: application/xml) »* — le fichier, lui, est valide. GitHub Pages sert bien le `.xml` en `application/xml`.
- **`robots.txt` fait autorité depuis le passage au domaine propre** : le site étant servi à la **racine** de `kingshottoolbox.com`, `https://kingshottoolbox.com/robots.txt` est bien celui que lisent les robots. Ce n'était **pas** le cas sous `/Kingshot_Toolbox/`, où seul le `robots.txt` de la racine `github.io` comptait (piège historique). Le sitemap se soumet malgré tout **directement dans Google Search Console**.
- **Favicon = 3 `<link>` en dur dans le `<head>` de chaque page** (`img/logo/favicon.svg` + `favicon-32.png` + `apple-touch-icon.png`, chemins relatifs, compatibles `<base href>`). Google lit le favicon avec un robot séparé **qui n'exécute pas le JavaScript** : tant que les balises n'étaient qu'injectées par `header.js`, l'icône affichée dans les résultats de recherche restait celle de GitHub. Le bloc de `header.js` est conservé en **filet de sécurité** (il ne s'exécute que si la page n'a aucun `link[rel=icon]`) — mais toute nouvelle page doit porter les **4** balises dans son HTML (svg + 96 + 32 + apple-touch). **Google exige un favicon d'au moins 48×48 (multiple de 48)** : c'est le rôle de `favicon-96.png` — le SVG est accepté mais le PNG 96 sécurise tous les crawlers. Les PNG se régénèrent depuis `favicon.svg` (cairosvg), y compris `favicon.ico` racine (16+32+48).
- **Un seul favicon par *hostname*** : Google le lit sur la page d'accueil du domaine. Avec `kingshottoolbox.com`, le site **est** cette page d'accueil — l'icône déclarée dans le `<head>` d'`index.html` vaut donc pour tout le domaine. Sous `aistra91-ks.github.io/Kingshot_Toolbox/`, c'était la racine `github.io` qui décidait, d'où le globe GitHub longtemps affiché dans les résultats de recherche.
- **Boutiques : deux valorisations, jamais mises en rapport.** Les pages boutique offrent deux lectures indépendantes (pastilles 💵 $ / € et 💎 Gemmes, `spViewTabsHtml()`). **C'est l'argent réel qui s'affiche à la première visite** : un prix en euros ou en dollars se lit sans connaître le jeu, là où la gemme suppose déjà de savoir ce qu'elle vaut. La bascule **substitue** les colonnes (`Valeur gemmes`→`Valeur €`, `Ratio (×N)`→`Ratio`, `Valeur`→`Valeur € tot.`), elle n'en **ajoute aucune** : le tableau garde sa largeur et son comportement mobile. **Aucun taux de change gemme↔euro n'est calculé nulle part** — les deux échelles ne se comparent pas. La vue vit dans l'URL pour qu'un lien partagé ouvre la bonne lecture, et se retient dans la clé chrome `shop_view` (cf. §8) : seule la lecture **non par défaut** s'y écrit (`?v=gem` aujourd'hui), les deux valeurs restant acceptées en entrée — les liens `?v=eur` déjà partagés continuent donc de fonctionner. Le tri suit la bascule (`SP_TWIN`), sinon le tableau resterait trié sur une colonne disparue.
- **Règle du « — » (non négociable).** Une valeur € absente affiche **`—`** en Valeur €, en Ratio et en Valeur € tot., et **sort du classement** : pas de barre, jamais « Top », rejetée en fin de tri dans les deux sens. **Jamais `0`** : un 0 placerait la ligne en *pire affaire* alors que la vérité est « inconnu », et inverserait le podium des boutiques mal couvertes (Arène : 9 objets valorisés sur 19). La ligne reste achetable — « Je prends » compris — elle ne crédite simplement aucune valeur, et la tuile « Valeur obtenue » **annonce la couverture** (« sur 19 des 21 objets valorisés »). En code : `scEurUnit()` renvoie `null`, jamais `0`.
- **Les deux référentiels se renvoient l'un à l'autre.** `shop/items.html` (gemmes) et `shop/items-euro.html` (argent réel) listent les mêmes objets sur deux échelles indépendantes : chacune porte en bas de page un `.sx-reflinks` vers l'autre, le composant déjà posé au bas des pages boutique. **Écrit en dur dans le HTML**, comme tout le maillage interne — c'est ce que parcourent les moteurs de recherche, et un lien construit en JS ne compterait pas. Le `data-i18n` va sur le `<span>` du libellé et jamais sur le `<a>` : `applyI18n` écrit un `textContent`, ce qui effacerait la pastille de l'icône.
- **Valeur corrigée = valeur affichée, mais jamais muette.** Trois blocs du relevé € (§7) peuvent décider d'un prix à la place du pack : `derived` (déduite d'un autre objet), `speedups` (barème à la minute) et `weights` (pondération assumée). `scEurUnit()` les renvoie **comme les autres** : elles comptent donc partout — page « Prix réel des objets », Valeur €, Ratio, podium et totaux de toutes les boutiques. En contrepartie **leur origine doit se voir partout** : une pastille dans la colonne « Pack d'origine » (`.ie-tag.is-derived` / `.is-scaled` / `.is-weighted`), l'encadré permanent et déplié sous le tableau (`#ie-derived`, `ieRenderRules()`) qui donne le raisonnement ET le calcul chiffré règle par règle, et l'info-bulle des pages boutique alimentée par `scEurWhy()` — qui rend le calcul, pas le seul mot « Calculé ». Un chiffre qu'on ne peut pas vérifier n'a rien à faire à côté d'un relevé. **La colonne « Pack d'origine » nomme le pack d'où vient le PRIX**, pas celui qui en donne le plus — les deux coïncident pour un objet relevé, plus du tout dès qu'une règle s'en mêle. Les cinq accélérateurs affichent donc le pack du **barème** (`basis.packs`, la Carte Hebdo Accélérer) et non chacun le sien : montrer « Offres Quotidiennes » sur le 3h laisserait croire que 6 € ÷ 12 y donne 0,151 €. Une valeur **déduite** n'affiche **aucun** pack — c'est un calcul, pas un relevé —, sauf si sa règle en nomme un dans `derived[id].packs` : le pack qui chiffre `fromId` vaut alors aussi pour elle (les 3 caisses de ressources, alignées sur le pain du Pack Lien Vital de la Ville). C'est `scEurPacks()` qui arbitre, et la recherche par pack suit le même arbitrage. Le calcul affiché repart des **chiffres relevés** (prix du pack, quantité) et non du prix unitaire arrondi : « 0,167 € × 100 » donnerait 16,70 € au lieu de 16,67 €. Une chaîne de dérivations est résolue mais **ne peut pas boucler** : `scEurResolve()` porte l'ensemble des objets déjà traversés et coupe le cycle.
- **Podium dédoublonné par `itemId` — dans le podium seulement.** Une boutique répète le même objet sur plusieurs lignes (3 × « Accélérateur Général 1h » et 2 × « Marteau de Forge » au Stand d'Aventure) : sans dédoublonnage, un top 3 afficherait deux fois le même objet. Le **tableau**, lui, doit garder toutes ses lignes.
- **Décimales adaptatives obligatoires sur la lecture €** (`scFmtEur` / `scFmtRatio`). Un `toFixed()` fixe écrase les petites valeurs : à 2 décimales les trois lignes d'Accélérateur 1h afficheraient le même chiffre. Valeur € : ≥100 → 0 déc. · ≥1 → 2 · ≥0,01 → 3 · sinon 4. Ratio : ≥10 → 1 · ≥0,1 → 3 · ≥0,001 → 4 · sinon **6** (Arène et Magasin des Marées descendent jusque-là). Le symbole se place selon la **langue** (« 35,94 € » en FR, « €35.94 » en EN), jamais selon la devise : mélanger les conventions (« $0,882 ») se lit comme une coquille.
- **Aperçu du pack (`shop/items-euro.html`) : trois entrées, un seul état.** Le nom d'un pack à image est un `<button>` qui ouvre sa capture. Trois chemins mènent là : **survol maintenu** (320 ms — sans délai, la bulle clignoterait à chaque traversée de la colonne), **focus clavier** (filtré par `:focus-visible`) et **clic**, qui est aussi le chemin du **toucher**, lequel n'a pas de survol. Piège rencontré : faire du clic une simple bascule de l'état visible **ne marche pas**, parce que la souris focalise aussi le bouton et que ce focus ouvrait déjà l'aperçu avant que le clic soit traité — la bascule lisait un état qu'un autre gestionnaire venait de changer. D'où `iePvPin`, qui mémorise l'**intention** (épinglé ou non) et non l'affichage. L'aperçu est en `position:fixed` attaché au `<body>` : sans cela le conteneur `overflow-x` du tableau le rognerait ; en contrepartie il ne suit pas la page, donc tout défilement le referme. Un pack en « Multipack » reste du texte inerte : plusieurs packs à égalité, aucune image à montrer.
- **Publier une version** : ajouter l'entrée **en tête** de `data/changelog.json` **et** porter `SITE.version` (site-config.js) au même numéro — c'est cette clé qu'affiche le pied de page. Les deux se font dans le même commit, sinon le site annonce une version qui n'existe pas dans l'historique. Une entrée de changelog et une annonce Discord couvrent le même périmètre : rédiger l'une en s'appuyant sur l'autre.
- **Notifications Discord** : plus aucun mapping à maintenir. Un nouvel outil n'exige aucune modification de `.github/` — il apparaîtra dans la prochaine annonce rédigée.
- **Tout champ de formulaire porte un nom accessible**, et ce nom **suit la langue**. Trois moyens, dans cet ordre : `for="<id>"` sur le libellé quand il en précède un seul (65 champs) ; `aria-labelledby="<id du libellé>"` quand le libellé est partagé par deux champs ou placé après (TrueGold Loup Gris, curseur d'éclats de la Caserne, niveau du familier) — il pointe un libellé déjà traduit par `data-i18n`, donc **le nom se traduit tout seul, sans nouvelle plomberie i18n** ; `aria-label` construit avec le dictionnaire actif (`tx`) pour les champs **générés** (les 16 listes et les cases du tableau TrueGold). Ne jamais se contenter de `title` : il donne bien un nom, mais le même pour toutes les lignes — seize cases annoncées « Inclure dans les suggestions » sans dire de quel bâtiment. Mesure : **0 champ sans nom sur 179**, EN et FR.
- **Clés localStorage** : toujours passer par `STORAGE_KEYS` + `safeParse` (jamais de chaîne littérale). Exception assumée : les clés « chrome » (`hub_theme`, `hub_lang`, `shop_view`, `shop_currency`, `fb_*`), qui ne sont ni de la donnée joueur ni du ressort des profils (cf. §8).
- **Retours joueurs : le formulaire se charge au premier clic.** `js/feedback.js` n'est référencé par **aucune** page — `header.js` l'injecte à la demande (`hdrOpenFeedback()`), ce qui évite d'ajouter une balise `<script>` aux 65 pages *et* de faire télécharger un formulaire à la quasi-totalité des visiteurs, qui ne l'ouvriront jamais. La promesse de chargement est mémorisée (deux clics rapides n'injectent pas deux scripts) et remise à `null` en cas d'échec, pour qu'un nouveau clic retente. **Deux détails de transport sans lesquels rien ne part** : l'envoi se fait en `Content-Type: text/plain` — un `application/json` déclencherait un préflight `OPTIONS` qu'Apps Script ne traite pas — et Apps Script répond **toujours** en HTTP 200, donc c'est le champ `ok` du corps qui dit si l'envoi a réussi, jamais le code HTTP. `SITE.feedback` (`url` + `key`) est **public par nature** : ces deux valeurs partent dans le JS servi à tout le monde et n'ouvrent rien d'autre que l'ajout d'une ligne ; l'adresse mail et le webhook Discord vivent dans le script Google, jamais dans le dépôt. `url` vide ⇒ ni bouton ni entrée de drawer (mieux vaut rien qu'un bouton qui échoue). L'anti-spam est **doublé** : ici (délai d'une minute, 5 par jour, champ piège) contre les envois accidentels et les robots naïfs, et côté script (plafond horaire) pour ce qui compte vraiment — ne jamais compter sur le premier seul, il est contournable.
- **Le bouton « Un retour ? » suit langue et thème dans le header adaptatif.** Il est masqué sous 820&nbsp;px et en `.hdr-condensed`, exactement comme eux : le drawer devient alors le **seul** chemin vers le formulaire, d'où l'entrée `.drawer-fb` qui n'est pas décorative. Conséquence mesurée sur `hdrEvaluateAdaptive()` : un bouton de plus dans la zone droite avance le seuil de condensation d'environ 48&nbsp;px — sans effet visible, mais à garder en tête avant d'en ajouter un quatrième.
- **i18n** : toute chaîne visible passe par un dictionnaire `{FR,EN}` + `data-i18n` (ou `data-en`/`data-fr` sur les pages bâtiments). Réagir à `langChanged`.
- **Icônes** : SVG Lucide **inline** (offline) via `SITE_ICONS`/`iconSvg()` (site-config) et `HEADER_ICONS`/`hdrSvg()` (header). Ajouter une nouvelle icône dans **les deux** registres si utilisée dans le header.
- **Images jeu** : WebP ; prévoir un fallback (`onerror`).
- **Header actif dans un sous-dossier** : définir `window.HDR_ACTIVE_HREF = '<href du manifeste>'` **avant** `header.js` (utilisé par toutes les pages `database/buildings/*`).
- **Modales & retours d'action** : utiliser `showAppAlert` / `showAppConfirm` / `showAppToast` (header.js), pas `alert()`/`confirm()`. Le toast (`.kt-toast`, jumeau visuel de `.pfp-toast`) sert aux confirmations *après* une action réussie — enchaîner deux modales à fermer ferait perdre de vue la page qui vient de changer.
- **DA** : respecter les variables CSS ; cartes = liseré doré + reflet au survol (cf. §5).
- **Commits** : toute modification touchant **plusieurs fichiers** se fait via **`github.dev`** et part en **un seul commit final** — jamais d'états intermédiaires cassés en ligne. Message **en anglais** : titre en langage courant + corps compréhensible par un joueur non développeur, dans le budget de longueur du §7.Dans `github.dev`, la boîte de message est multiligne (**Shift+Entrée**) : ligne 1 = titre, ligne vide, puis le corps. Tout mettre sur une seule ligne rend l'intégralité de la news **en gras** sur Discord (le corps devient partie du `subject`).

**Pièges déjà rencontrés**
- **Dépendance `GlobalLang` non définie** : `lang.js` doit être chargé **avant** tout script qui appelle `GlobalLang` ; toujours garder le fallback `window.GlobalLang ? GlobalLang.get() : 'FR'` (déjà en place dans header/help/backup). Ordre de chargement critique.
- **Fonction `getRawNumber` manquante** : helper local à `beartrap.js` (nettoie espaces + `parseInt`). N'existe **pas** globalement — ne pas l'appeler depuis un autre script sans la (re)définir.
- **Formatage des nombres dépendant de la locale** : l'UI formate les milliers via `toLocaleString('fr-FR')` (espaces). Les inputs numériques FR **refusent le `.`** ; côté saisie, nettoyer (`replace(/\D/g,'')` / strip espaces) **avant** `parseInt`/`parseFloat`. Ne pas parser directement `el.value` formaté.
- **Gestion d'erreurs des `fetch`** : tous les chargements de données sont des `fetch('data/….json')` relatifs (sensibles au chemin de déploiement Pages). Toujours vérifier `response.ok` et prévoir un message clair (ex. TrueGold affiche un diagnostic « le fichier existe-t-il ? »). Un `data/` mal résolu casse silencieusement l'outil.
- **Embeds Discord fusionnés** : deux embeds d'un même message qui portent la **même `url`** sont regroupés par Discord en un seul bloc, et la description du second est perdue (c'est le mécanisme d'affichage multi-images). Ne jamais mettre `url` sur les embeds FR/EN de `announce.js` — le lien vers le site passe par `author`.
- **Équipement Exclusif d'un héros (`widget`)** — trois pièges, tous silencieux :
  1. **5 paliers pour un sélecteur qui va à 10** : la Conquête se lit aux niveaux **impairs** (1/3/5/7/9), l'Expédition aux **pairs** (2/4/6/8/10) — `getWidgetEffectValue()` (`caserne.js`). Un `levels[]` de 10 entrées ferait donc afficher une valeur sur deux. Au niveau 0 (et 1 côté Expédition) l'effet vaut `"0%"` et s'affiche grisé.
  2. **Substitution des « X »** (`formatWidgetDesc`, `caserne.js`) : une phrase à **une** valeur voit **tous** ses « X »/« X% » remplacés (regex globale) — ne jamais laisser un X littéral ailleurs dans le texte, dans aucune des deux langues. Une phrase à **deux** valeurs utilise le format `"(a,b)"` — **virgule**, contrairement au `"(a;b)"` des Experts (§6) — et les valeurs sont injectées **dans l'ordre du texte**. Le `%` fait partie de la valeur : écrire `X%` dans la phrase et `"15%"` dans `levels`.
  3. **Les icônes sont résolues par `name.EN` exact** : `img/widgetname/<widget.name.EN>.webp` (l'arme) et `img/widgetskill/<effect.name.EN>.webp` (les 2 effets). Renommer ou corriger un `name.EN` casse l'image sans erreur — d'où la coquille `Offenseive Defense` (Yang) qu'il faut **conserver** telle quelle.
  Un héros sans `widget` ne casse rien : `renderModalWidget()` masque le conteneur, et `modal-tabs.js` retire l'onglet « Équipement » du mode mobile (panneau vide). *C'est ce qui a rendu invisible pendant un temps l'absence des équipements gen 7.*

---

*Fin de MAP.md — proposer une mise à jour ciblée de ce fichier à chaque changement de fichiers/architecture.*

---

## 10. Tests

**`node --test`** à la racine du dépôt. Rien à installer, rien à construire — c'est le lanceur de Node lui-même, et les tests ne chargent que des fichiers déjà servis par le site.

**Comment ça marche.** Le site n'a ni modules ni build : `js/*.js` pose des fonctions globales que le navigateur assemble page par page. `tests/harness.mjs` reproduit ça dans un contexte `vm` — les scripts y sont chargés **sans être modifiés**, dans l'ordre du HTML, avec trois substituts : `fetch` lit `data/` sur le disque, `localStorage` est en mémoire (donc chaque test part des valeurs par défaut), et `document.getElementById` rend `null`, ce qui fait sortir les IIFE de fin de fichier avant tout rendu. C'est ce qui permet de tester **le code réellement servi** sans lui ajouter le moindre `export`.

Conséquence à connaître : les `let` de premier niveau d'un script vivent dans la portée lexicale du contexte, pas sur son objet global. On ne les lit et ne les écrit donc qu'**à l'intérieur** du contexte, via `run(ctx, '…')`.

**Ce qui est couvert.** Les deux calculs où une régression serait invisible à l'écran et coûteuse pour le joueur :
- **Valorisation d'événement** (`seCompute`) — les contrôles réels de §7, jusqu'ici vérifiés à la main une seule fois : Caravane du Dragon 4 jours + achat déclaré → **230 essences** (et 195 sans, ce qui prouve d'où viennent les 35), Stand d'Aventure F2P 5 jours → **32 388 gemmes**, plus l'invariant « autant de Points de Vente que de Pièces d'Aventure par source ».
- **Tirage du Théâtre** (`ftClimbCostFrom`, `ftComputeReach`, `ftAttemptsFrom`) — la forme close comparée à une **simulation Monte-Carlo qui ne partage aucune ligne avec elle**. Le générateur est **grainé** : à graine fixe le résultat ne bouge pas, donc un test qui échoue signale un vrai écart, jamais un coup de malchance.

**Ajouter un test.** Un contrôle chiffré écrit en prose dans ce fichier est déjà un test — il ne lui manque que d'être exécutable. Le transcrire vaut mieux que d'en inventer un nouveau : il vient d'un relevé en jeu.

**Une règle.** Un test qui ne peut pas échouer ne protège rien. Après en avoir écrit un, casser volontairement le code qu'il couvre et vérifier qu'il vire au rouge — c'est ce qui a validé la première série.

---

## 11. Pages générées

**Rédaction des 17 pages boutique** : titre, description et intro présentent les **deux lectures** — gemmes et €/$ — parce que l'affichage s'ouvre en €/$ (`scChromeGet(SC_VIEW_KEY,'eur')`). Auparavant les 17 promettaient les gemmes seules, et le visiteur arrivait sur des prix en euros. La phrase ajoutée aux intros est identique partout (« La page s'ouvre sur la lecture €/$ et bascule en gemmes d'un clic ») et **reportée à la main sur le jumeau FR**, qui n'est pas généré. Vérifié avant rédaction : la couverture euro va de **63 % (Arène) à 100 %** selon la boutique — la mention est donc exacte partout.

**25 des 70 pages ne s'écrivent plus à la main** : les 17 pages boutique et les 8 pages de bâtiments sortent d'un gabarit.

```
python3 tools/build_pages.py           écrit les pages
python3 tools/build_pages.py --check   ne touche à rien, échoue si une page servie
                                       ne correspond plus à son gabarit (lancé en CI)
```

**Ça ne contredit pas « aucun build ».** Le script tourne **à la main, avant le commit**, et **sa sortie est commitée** : GitHub Pages sert exactement le même HTML statique et complet qu'avant, donc la règle du §9 (le contenu doit exister sans exécuter le JS) est intacte. Ce qui disparaît, c'est d'écrire 17 fois la même chose : avant ce générateur, 78 lignes étaient identiques dans ≥15 des 19 pages boutique, et deux pages de bâtiments ne différaient que de **24 lignes sur 654**.

**Où éditer quoi.**

| Ce que tu veux changer | Le fichier à ouvrir |
|---|---|
| Le gabarit commun (head, scripts, structure) | `tools/templates/shop.html` ou `building.html` |
| Titre, description, nom FR/EN, intro d'une page | `tools/pages-shop.json` / `pages-building.json` |
| Un bloc propre à UNE page | `tools/partials/<slug>.<rôle>.html` |
| **Une page `shop/*.html` ou `database/buildings/*.html`** | **aucun — elle est regénérée, l'édition serait perdue** |

**Ce qui se déduit ne se déclare pas.** Une boutique charge `shop-event.js` **et** reçoit son `<div id="sp-event">` parce qu'elle a un `data/events/<slug>.json` ; elle affiche « Chest contents » parce qu'elle est listée dans `shopcalc_chests.json` ; la nav des bâtiments suit l'ordre de `pages-building.json`. Aucune de ces informations n'est répétée dans le JSON — donc aucune ne peut s'y désynchroniser.

**Le jumeau français n'est pas généré non plus, et c'est surveillé.** `fr/shop/theater-shop.html` est écrite à la main : texte français en dur (aucun `data-en`/`data-fr`), `<base href="../../">`, `hreflang` inversés — elle n'entre pas dans le gabarit sans y ajouter une dimension de langue, ce qui n'a pas lieu d'être tant que le pilote ne concerne qu'une page. Le risque, lui, est réel : une modification du gabarit passe sur l'anglaise et pas sur la française. `--check` compare donc ce dont la dérive **casse** vraiment la page — la liste des scripts et des feuilles de style des deux jumelles — et échoue si elles divergent. C'est exactement ce qui a déjà dû être rattrapé deux fois à la main. Généraliser le pilote (cf. §4) rendrait le gabarit bilingue et supprimerait le garde-fou.

**Ni `tools/` ni `tests/` ne sont publiés** : `_config.yml` les exclut. Sans cette exclusion, `tools/templates/shop.html` serait servi tel quel, `{{EMPLACEMENTS}}` compris, et les huit `tools/partials/*.table.html` exposeraient le tableau complet des pages bâtiments sous forme de fragments sans `<head>` ni canonique.

**Ne sont pas générées** : `shop/items.html`, `shop/items-euro.html` et `database/buildings/index.html` — trois pages uniques, avec leur propre corps et leurs propres scripts. Les mettre dans un gabarit à un exemplaire ne rapporterait rien.

**Les tableaux de niveaux des bâtiments restent des partials**, recopiés tels quels depuis les pages existantes. Ils pourraient venir de `data/truegold_db.json` (`dbDataRaw` porte les mêmes chiffres), mais les libellés FR des prérequis et des durées n'existent que dans le HTML : les reconstruire risquerait de modifier en silence des données publiées. C'est un chantier suivant, à mener en déplaçant d'abord ces traductions dans la donnée.
